{
  "generator": "scripts/review-loop-record.mjs",
  "generatedAt": "2026-08-28T04:13:21.532Z",
  "evidenceCapturedAt": "2026-08-28T04:12:53Z",
  "pr": 585,
  "title": "Stop the override badge firing on every fact's required Visual Concept",
  "artifact": {
    "files": 6,
    "added": 368,
    "removed": 13,
    "patch": "diff --git a/.agents/adjudications/585-1.json b/.agents/adjudications/585-1.json\nnew file mode 100644\nindex 0000000..151de18\n--- /dev/null\n+++ b/.agents/adjudications/585-1.json\n@@ -0,0 +1,170 @@\n+{\n+  \"generator\": \"scripts/review-loop-record.mjs\",\n+  \"generatedAt\": \"2026-08-28T03:56:02.524Z\",\n+  \"evidenceCapturedAt\": \"2026-08-28T03:55:32Z\",\n+  \"pr\": 585,\n+  \"title\": \"Stop the override badge firing on every fact's required Visual Concept\",\n+  \"artifact\": {\n+    \"files\": 5,\n+    \"added\": 305,\n+    \"removed\": 13,\n+    \"patch\": \"diff --git a/.agents/receipts/loop-budget-585.json b/.agents/receipts/loop-budget-585.json\\nnew file mode 100644\\nindex 0000000..ba92014\\n--- /dev/null\\n+++ b/.agents/receipts/loop-budget-585.json\\n@@ -0,0 +1,8 @@\\n+{\\n+  \\\"pr\\\": 585,\\n+  \\\"tier\\\": \\\"product\\\",\\n+  \\\"budget\\\": 5,\\n+  \\\"criticality\\\": 55,\\n+  \\\"artifact\\\": \\\"Exclude the required Visual Concept from the has-been-overridden signal (bugfix, Tier B)\\\",\\n+  \\\"declaredAt\\\": \\\"2026-08-28T03:13:05.464Z\\\"\\n+}\\ndiff --git a/artifacts/api-server/src/__tests__/enrichmentOverridesResolver.test.ts b/artifacts/api-server/src/__tests__/enrichmentOverridesResolver.test.ts\\nindex bd7f7da..2f80df7 100644\\n--- a/artifacts/api-server/src/__tests__/enrichmentOverridesResolver.test.ts\\n+++ b/artifacts/api-server/src/__tests__/enrichmentOverridesResolver.test.ts\\n@@ -104,11 +104,23 @@ describe(\\\"resolveEnrichment\\\", () => {\\n       overrides: {},\\n       visualPromptStrategyOverride: legacy,\\n     });\\n-    // The override still counts as renderable content — its core scene is set.\\n-    assert.equal(summary.hasVisualStrategyOverride, true);\\n+    // The core scene is preserved verbatim...\\n     assert.equal(effective.visualPromptStrategyOverride?.coreSceneOverride, \\\"a giant lifting the earth\\\");\\n     // ...and the full-schema parse fills the absent lists back in.\\n     assert.deepEqual(effective.visualPromptStrategyOverride?.requiredVisualDetails, []);\\n+    // ...but a core scene alone is the REQUIRED Visual Concept, not a moderator\\n+    // override, so it does not raise the signal (#584). The #579 regression this\\n+    // test exists for is the absence of a throw above, not this flag.\\n+    assert.equal(summary.hasVisualStrategyOverride, false);\\n+  });\\n+\\n+  // Regression — #584. The blob shape above is what EVERY production-ready fact\\n+  // carries, so counting it made the signal true for the whole corpus.\\n+  it(\\\"raises the signal for a legacy blob that also carries real override content\\\", () => {\\n+    const legacy = { version: 1, coreSceneOverride: \\\"a giant lifting the earth\\\", requiredVisualDetails: [\\\"a red hat\\\"] } as unknown as\\n+      FactEnrichment[\\\"visualPromptStrategyOverride\\\"];\\n+    const { summary } = resolveEnrichment({ aiDerived: AI, overrides: {}, visualPromptStrategyOverride: legacy });\\n+    assert.equal(summary.hasVisualStrategyOverride, true);\\n   });\\n \\n   it(\\\"resolves a stored visual override that is a bare version stub\\\", () => {\\n@@ -126,16 +138,35 @@ describe(\\\"resolveEnrichment\\\", () => {\\n // is why the collector itself is guarded too. Imported from the api-server lib\\n // rather than api-zod deliberately: this pins the caller, not the resolver.\\n describe(\\\"serializeResolved — the caller that bypasses resolveEnrichment (#579)\\\", () => {\\n-  it(\\\"does not crash on a legacy visual override when there is no AI baseline\\\", () => {\\n-    const legacy = { version: 1, coreSceneOverride: \\\"{NAME} stands there confidently.\\\" } as unknown as\\n-      FactEnrichment[\\\"visualPromptStrategyOverride\\\"];\\n-    const out = serializeResolved({\\n+  const serializeWith = (visual: unknown) =>\\n+    serializeResolved({\\n       aiDerived: null,\\n       overrides: {},\\n       effective: null,\\n       enrichmentStatus: \\\"ready\\\",\\n-      visualPromptStrategyOverride: legacy,\\n+      visualPromptStrategyOverride: visual as FactEnrichment[\\\"visualPromptStrategyOverride\\\"],\\n     }) as { overrideSummary: { hasVisualStrategyOverride: boolean } };\\n+\\n+  it(\\\"does not crash on a legacy visual override when there is no AI baseline\\\", () => {\\n+    // The #579 regression is that this returns at all rather than throwing.\\n+    const out = serializeWith({ version: 1, coreSceneOverride: \\\"{NAME} stands there confidently.\\\" });\\n+    assert.equal(typeof out.overrideSummary.hasVisualStrategyOverride, \\\"boolean\\\");\\n+  });\\n+\\n+  // Regression — #584, on the bypassing caller specifically: this path computes\\n+  // the signal itself rather than reading the resolver's, so it needs its own\\n+  // proof that a Visual-Concept-only blob does not raise it.\\n+  it(\\\"does not raise the signal for a Visual-Concept-only blob (no AI baseline)\\\", () => {\\n+    const out = serializeWith({ version: 1, coreSceneOverride: \\\"{NAME} stands there confidently.\\\" });\\n+    assert.equal(out.overrideSummary.hasVisualStrategyOverride, false);\\n+  });\\n+\\n+  it(\\\"still raises the signal when that same blob carries real override content\\\", () => {\\n+    const out = serializeWith({\\n+      version: 1,\\n+      coreSceneOverride: \\\"{NAME} stands there confidently.\\\",\\n+      bubbles: [{ type: \\\"speech\\\", entity: \\\"subject\\\", text: \\\"Not again.\\\" }],\\n+    });\\n     assert.equal(out.overrideSummary.hasVisualStrategyOverride, true);\\n   });\\n });\\ndiff --git a/artifacts/api-server/src/__tests__/visualStrategyOverride.test.ts b/artifacts/api-server/src/__tests__/visualStrategyOverride.test.ts\\nindex bc58766..07f6f01 100644\\n--- a/artifacts/api-server/src/__tests__/visualStrategyOverride.test.ts\\n+++ b/artifacts/api-server/src/__tests__/visualStrategyOverride.test.ts\\n@@ -200,10 +200,43 @@ describe(\\\"token helpers\\\", () => {\\n describe(\\\"hasRenderableVisualStrategyOverrideContent\\\", () => {\\n   const asOv = (partial: Record<string, unknown>) => makeOverride(partial) as unknown as VisualPromptStrategyOverride;\\n \\n-  it(\\\"counts coreSceneOverride as renderable content\\\", () => {\\n+  // `coreSceneOverride` is the storage for the REQUIRED Visual Concept, not a\\n+  // moderator override: a fact cannot be saved or released to production with a\\n+  // blank one (VisualConceptCard, and 8 required-gates across the admin/review\\n+  // routes). So its presence says \\\"this fact reached concept review\\\", which is\\n+  // universal — it can never distinguish a fact a moderator intervened on. It is\\n+  // excluded from this signal while staying fully rendered/tokenized (#584).\\n+  it(\\\"does NOT count coreSceneOverride — it is the required Visual Concept, not an override\\\", () => {\\n     assert.equal(hasRenderableVisualStrategyOverrideContent(asOv({})), false);\\n     assert.equal(hasRenderableVisualStrategyOverrideContent(asOv({ coreSceneOverride: \\\"  \\\" })), false);\\n-    assert.equal(hasRenderableVisualStrategyOverrideContent(asOv({ coreSceneOverride: \\\"{NAME} on a throne\\\" })), true);\\n+    assert.equal(\\n+      hasRenderableVisualStrategyOverrideContent(asOv({ coreSceneOverride: \\\"{NAME} on a throne\\\" })),\\n+      false,\\n+    );\\n+    // The exact production blob from review #6880 / fact 22587 (#584).\\n+    assert.equal(\\n+      hasRenderableVisualStrategyOverrideContent(\\n+        { version: 1, coreSceneOverride: \\\"{NAME} stands there confidently.\\\" } as unknown as VisualPromptStrategyOverride,\\n+      ),\\n+      false,\\n+    );\\n+  });\\n+\\n+  it(\\\"still counts every other rendered field when a Visual Concept is also present\\\", () => {\\n+    // The negative case that matters: a core scene must not MASK real override\\n+    // content, and each field must count independently of it.\\n+    const withScene = (partial: Record<string, unknown>) =>\\n+      hasRenderableVisualStrategyOverrideContent(\\n+        asOv({ coreSceneOverride: \\\"{NAME} stands there confidently.\\\", ...partial }),\\n+      );\\n+    assert.equal(withScene({ requiredVisualDetails: [\\\"a red hat\\\"] }), true);\\n+    assert.equal(withScene({ forbiddenVisualDetails: [\\\"no logos\\\"] }), true);\\n+    assert.equal(withScene({ compositionGuidance: [\\\"wide shot\\\"] }), true);\\n+    assert.equal(withScene({ styleAgnosticPromptAdditions: [\\\"grainy\\\"] }), true);\\n+    assert.equal(withScene({ negativePromptAdditions: [\\\"blurry\\\"] }), true);\\n+    assert.equal(withScene({ roleBindings: [{ entity: \\\"the bartender\\\", visualRole: \\\"pouring\\\" }] }), true);\\n+    assert.equal(withScene({ violencePolicyOverride: { mode: \\\"soften\\\", intensity: \\\"mild\\\" } }), true);\\n+    assert.equal(withScene({ subjectRealizationOverride: { mode: \\\"subject_as_object\\\", description: \\\"\\\" } }), true);\\n   });\\n \\n   it(\\\"ignores admin-only fields (moderatorIntent, notesForModerator)\\\", () => {\\n@@ -213,6 +246,52 @@ describe(\\\"hasRenderableVisualStrategyOverrideContent\\\", () => {\\n     );\\n   });\\n \\n+  // Guards the scoping of the #584 fix: excluding the Visual Concept from the\\n+  // \\\"was this overridden\\\" SIGNAL must not remove it from the RENDER/TOKENIZE\\n+  // path, which is a different question answered by a different function.\\n+  it(\\\"leaves coreSceneOverride fully rendered and tokenized (signal ≠ render path)\\\", () => {\\n+    const ov = asOv({ coreSceneOverride: \\\"{NAME} on a throne\\\" });\\n+    const entry = collectRenderedTextEntries(ov).find((e) => e.path === \\\"coreSceneOverride\\\");\\n+    assert.ok(entry, \\\"coreSceneOverride must still be emitted as rendered text\\\");\\n+    assert.equal(entry!.value, \\\"{NAME} on a throne\\\");\\n+    assert.equal(entry!.kind, \\\"prose\\\");\\n+    assert.equal(isVisualStrategyRenderedTextPath(\\\"coreSceneOverride\\\"), true);\\n+    // Token validation on the Visual Concept must still fire.\\n+    assert.ok(firstOverrideTokenError(asOv({ coreSceneOverride: \\\"{NOPE} on a throne\\\" })));\\n+    assert.equal(firstOverrideTokenError(ov), null);\\n+  });\\n+\\n+  // Regression — #584 round 2. Same class as the coreSceneOverride bug: a field\\n+  // counted as override evidence that the compiler does not actually render.\\n+  // `composeSubjectRealization` (nanoBanana2.ts:511) returns \\\"\\\" whenever the mode\\n+  // is `use_ai_plan`, whatever the description says — so a description parked\\n+  // under the default mode is a no-op that must not raise the signal.\\n+  it(\\\"does NOT count a subjectRealizationOverride description under the default mode\\\", () => {\\n+    assert.equal(\\n+      hasRenderableVisualStrategyOverrideContent(\\n+        asOv({ subjectRealizationOverride: { mode: \\\"use_ai_plan\\\", description: \\\"a stern librarian\\\" } }),\\n+      ),\\n+      false,\\n+    );\\n+    // ...and it still counts the moment the mode is anything else, description or not.\\n+    assert.equal(\\n+      hasRenderableVisualStrategyOverrideContent(\\n+        asOv({ subjectRealizationOverride: { mode: \\\"subject_as_object\\\", description: \\\"a stern librarian\\\" } }),\\n+      ),\\n+      true,\\n+    );\\n+    // A real override elsewhere is not masked by the inert realization block.\\n+    assert.equal(\\n+      hasRenderableVisualStrategyOverrideContent(\\n+        asOv({\\n+          subjectRealizationOverride: { mode: \\\"use_ai_plan\\\", description: \\\"a stern librarian\\\" },\\n+          requiredVisualDetails: [\\\"a red hat\\\"],\\n+        }),\\n+      ),\\n+      true,\\n+    );\\n+  });\\n+\\n   it(\\\"counts policy overrides and non-default subject realization\\\", () => {\\n     assert.equal(\\n       hasRenderableVisualStrategyOverrideContent(asOv({ violencePolicyOverride: { mode: \\\"soften\\\", intensity: \\\"mild\\\" } })),\\ndiff --git a/docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md b/docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\\nnew file mode 100644\\nindex 0000000..c76e59c\\n--- /dev/null\\n+++ b/docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\\n@@ -0,0 +1,135 @@\\n+# PR #585 — The \\\"Overridden\\\" badge only shows when someone actually overrode something — UAT\\n+\\n+**Workstream:** #584\\n+\\n+The admin enrichment editor was telling you a fact had a moderator override\\n+when it had none. The badge keyed off the Visual Concept — the \\\"describe the\\n+picture\\\" text — which every fact is *required* to have. So it lit up on\\n+essentially every fact that had reached concept review, which is most of them,\\n+and the one signal meant to say \\\"a human touched this one\\\" said it about\\n+everything.\\n+\\n+You found this yourself during PR #582's UAT: review #6880 showed\\n+**\\\"Overridden: Visual Strategy\\\"** while its entire Visual Strategy Override\\n+panel was empty. Your call was that the Visual Concept should be omitted from\\n+the check, and that is exactly what shipped.\\n+\\n+**The risk this run is really checking** is the other direction. The Visual\\n+Concept is still sent to the image engine and still has its `{NAME}` tokens\\n+validated — it just no longer counts as *evidence of an override*. Those are\\n+two different jobs for one field, and the way this fix could have gone wrong is\\n+by removing the field from both. **Steps 3 and 4 are the load-bearing ones**:\\n+they confirm the picture-prompt still carries the scene and tokens are still\\n+checked. Steps 1 and 2 confirm the badge itself.\\n+\\n+Steps 1–4 and R1–R2 are read-only. **Step 5 is the only one that writes**, and\\n+it is written to undo itself.\\n+\\n+## Setup\\n+\\n+- [claude] Confirm `main` is synced to the Repl and the checked-out SHA matches the merge commit, before anything is read.\\n+- [claude] Capture the count of facts carrying a Visual Concept — the size of the set that was showing the incorrect badge — so \\\"it's fixed\\\" has a number behind it.\\n+- [claude] Identify and name in the preview: one fact whose override panel is genuinely empty (review #6880 is the known case), and one with real override content. Steps 1, 2, 5 and R2 each need a known subject rather than a hunt.\\n+- [claude] Before step 5 runs, capture the current stored `visualPromptStrategyOverride` for the fact step 5 will edit — including its `updatedBy` and `updatedAt` — and record it in the run record, so the restore below has a real captured value rather than a guess.\\n+- [david] Sign in to the admin console as yourself; every step is an admin screen.\\n+- [restore] The visual override on the fact edited in step 5 — restore it to the value captured before that step, **after every run, not only an interrupted one**. Removing the scratch entry in the step restores the *content* but not the provenance: each save that changes content re-stamps `updatedBy`/`updatedAt`, so without this the fact is left recorded as last edited by the tester at test time.\\n+\\n+## Steps\\n+\\n+### 1. The badge is gone where nothing was overridden\\n+\\n+**Do:** Go to **Admin → Moderation** and open the review I name in the preview\\n+(the fact with an empty override panel — review #6880 unless I say otherwise).\\n+Expand **Advanced Options** and look at the **AI Visual Classification** block,\\n+just under the amber \\\"Ambiguous sentence-initial entity\\\" note.\\n+\\n+**Expect:** There is **no** orange `Overridden: Visual Strategy` bar. The\\n+classification fields below it (Joke Mechanism, Depiction Style, Overhype Fit\\n+and so on) are unchanged and still populated.\\n+\\n+### 2. The badge still appears where something really was overridden\\n+\\n+**Do:** Open the review I name in the preview as having real override content —\\n+a required visual detail, a speech bubble, or a composition note. Expand\\n+**Advanced Options** and look at the same **AI Visual Classification** block.\\n+\\n+**Expect:** The orange `Overridden: Visual Strategy` bar **is** shown. This is\\n+the case that must not have been broken by the fix.\\n+\\n+### 3. The Visual Concept still reaches the picture prompt\\n+\\n+**Do:** On the same review as step 2, read the text in the **Visual Concept —\\n+describe the picture** field, then open **Prompt Diagnostics** in the same\\n+Advanced Options section. **Click \\\"Generate runtime prompt preview\\\" and wait for\\n+it to finish**, then read the compiled prompt. Do not read whatever is already\\n+on screen when the panel opens — the panel restores the last saved result for\\n+this fact without recomputing, which on a browser that has opened it before\\n+would be a prompt built by the *old* code. (Generating is read-only: the panel\\n+computes a preview and does not render or save.)\\n+\\n+**Expect:** The compiled prompt contains a **CORE SCENE** section carrying the\\n+Visual Concept text you just read. The Visual Concept is still reaching the\\n+engine — which is what the fix had to avoid breaking.\\n+\\n+### 4. A bad token in the Visual Concept is still caught\\n+\\n+**Do:** In the **Visual Concept — describe the picture** field on any fact, type\\n+`{NOPE}` at the end of the existing text. **Do not save.**\\n+\\n+**Expect:** A token warning appears under the field flagging the unknown token,\\n+the same validation as before this PR. **Then delete the `{NOPE}` you typed**,\\n+leaving the field exactly as you found it, and close the editor without saving.\\n+\\n+### 5. Saving a real override raises the badge — then clearing it lowers it again\\n+\\n+**Do:** Open the fact I name in the preview as having an empty override panel.\\n+In **Advanced Options → Visual Strategy Override**, add one **Required visual\\n+detail** with the text `uat-585-scratch`, and save. Observe the badge. Then\\n+**remove that same entry and save again.**\\n+\\n+**Expect:** After the first save the `Overridden: Visual Strategy` bar\\n+**appears** — the badge tracks the override you just made. After removing the\\n+entry and saving again, the bar is **gone**. The distinctive text makes the\\n+entry unmistakable if anything is left behind; tell me if the second save does\\n+not clear it and I will restore from the value captured in setup.\\n+\\n+## Regression\\n+\\n+### R1. The enrichment editor still opens on a normal fact\\n+\\n+**Do:** Open **Admin → Moderation**, pick any fact with completed enrichment,\\n+and open its enrichment editor.\\n+\\n+**Expect:** The editor loads with its classification fields populated —\\n+archetype, subtype, fit — and no error banner.\\n+\\n+### R2. Real override content still reaches the prompt alongside the scene\\n+\\n+**Do:** On the fact from step 2 (the one with real override content), open\\n+**Prompt Diagnostics**, **click \\\"Generate runtime prompt preview\\\" and wait for\\n+it to finish** — same reason as step 3, a restored result may predate the merge\\n+— then read the compiled prompt.\\n+\\n+**Expect:** The prompt carries **both** the CORE SCENE **and** that fact's\\n+override content — its required detail, bubble, or composition note. This is\\n+the \\\"a core scene must not mask real override content\\\" invariant seen end to\\n+end rather than in a unit test.\\n+\\n+## Not bugs\\n+\\n+- **A fact whose override panel is empty shows no badge even though its Visual\\n+  Concept is long and detailed.** That is the entire point of the fix: the\\n+  Visual Concept is required on every fact, so it can never distinguish one\\n+  fact from another. Only the fields under **Visual Strategy Override** count.\\n+- **The Facts page \\\"Overridden\\\" filter is unchanged by this PR.** It filters on\\n+  the taxonomy override map (`enrichmentOverrides`), which is a different thing\\n+  from the visual override and is not touched here — so its result set should\\n+  look exactly as it did before. An earlier draft of this doc claimed the list\\n+  would shrink; that was wrong and there is nothing to check there.\\n+- **There is no way to tell an AI-drafted Visual Concept from one you wrote\\n+  yourself.** The field stores both identically and nothing records which it\\n+  was, which is *why* it is excluded rather than refined. If distinguishing\\n+  them is worth having, that is a product decision and a separate piece of\\n+  work, not a defect here.\\n+- **A recovered enrichment job still shows its old error text on Queue\\n+  Health.** Unrelated to this PR — a known observability gap recorded on #579.\\ndiff --git a/lib/api-zod/src/visualStrategyOverride.ts b/lib/api-zod/src/visualStrategyOverride.ts\\nindex 78355ac..8481cf7 100644\\n--- a/lib/api-zod/src/visualStrategyOverride.ts\\n+++ b/lib/api-zod/src/visualStrategyOverride.ts\\n@@ -465,18 +465,57 @@ export const visualPromptStrategyOverrideSchema = visualPromptStrategyOverrideBa\\n     });\\n   });\\n \\n+/** Rendered-text paths that are NOT evidence of moderator intervention for THIS\\n+ *  override, and so are excluded from `hasRenderableVisualStrategyOverrideContent`\\n+ *  while staying fully rendered and tokenized everywhere else.\\n+ *\\n+ *  Two reasons a path lands here, and they are different:\\n+ *\\n+ *  1. **Required of every fact.** `coreSceneOverride` holds the Visual Concept,\\n+ *     which a fact cannot be saved or released to production without (see\\n+ *     `VisualConceptCard`, and the required-gates in the admin/review routes).\\n+ *     Every production-ready fact carries one, so its presence says \\\"this fact\\n+ *     reached concept review\\\", never \\\"a moderator overrode something\\\" (#584). It\\n+ *     is also not a discriminator we could refine: the field holds the accepted\\n+ *     AI draft and a hand-written scene identically, with nothing stored to tell\\n+ *     them apart.\\n+ *  2. **Inert in this state.** A `subjectRealizationOverride.description` under\\n+ *     `mode: \\\"use_ai_plan\\\"` renders nothing — the compiler's\\n+ *     `composeSubjectRealization` returns \\\"\\\" for that mode whatever the\\n+ *     description says — so it is a no-op, not an override. (A non-default mode\\n+ *     is caught by the explicit check in the predicate below, description or\\n+ *     not.)\\n+ *\\n+ *  Both are the same underlying rule: this signal counts only content that both\\n+ *  reaches the engine AND represents a human decision. */\\n+function nonOverrideRenderedPaths(ov: VisualPromptStrategyOverride): ReadonlySet<string> {\\n+  const paths = new Set<string>([\\\"coreSceneOverride\\\"]);\\n+  if (!ov.subjectRealizationOverride || ov.subjectRealizationOverride.mode === \\\"use_ai_plan\\\") {\\n+    paths.add(\\\"subjectRealizationOverride.description\\\");\\n+  }\\n+  return paths;\\n+}\\n+\\n /**\\n  * True when the override carries any content that is RENDERED into the engine\\n- * prompt. The single source of truth for \\\"enabled but empty\\\" style checks so\\n- * UI surfaces can't drift on what counts as content. Admin-only fields\\n- * (moderatorIntent, notesForModerator) deliberately do NOT count.\\n+ * prompt **and** indicates a deliberate moderator override. The single source of\\n+ * truth for \\\"enabled but empty\\\" style checks so UI surfaces can't drift on what\\n+ * counts as content. Admin-only fields (moderatorIntent, notesForModerator)\\n+ * deliberately do NOT count, and neither do `nonOverrideRenderedPaths` above.\\n+ *\\n+ * This is the \\\"was this overridden\\\" SIGNAL, a different question from \\\"what gets\\n+ * rendered\\\" — which stays `collectRenderedTextEntries`, deliberately untouched\\n+ * by the exclusions above so tokenization and prompt compilation are unaffected.\\n  */\\n export function hasRenderableVisualStrategyOverrideContent(\\n   ov: VisualPromptStrategyOverride,\\n ): boolean {\\n   if (ov.supportingTextPolicyOverride || ov.violencePolicyOverride) return true;\\n   if (ov.subjectRealizationOverride && ov.subjectRealizationOverride.mode !== \\\"use_ai_plan\\\") return true;\\n-  return collectRenderedTextEntries(ov).some(({ value }) => value.trim().length > 0);\\n+  const excluded = nonOverrideRenderedPaths(ov);\\n+  return collectRenderedTextEntries(ov).some(\\n+    ({ path, value }) => !excluded.has(path) && value.trim().length > 0,\\n+  );\\n }\\n \\n /** An empty override scaffold (all lists empty). Under presence-based activation\",\n+    \"patchNote\": \"base...head: the artifact this round's findings are about. This is the diff to read when the rubric asks whether a finding describes a critical flaw. `sinceLastReview.patch` is separate and is normally empty under the write-gate rule, since the judge rules on an already-reviewed head.\"\n+  },\n+  \"budget\": {\n+    \"tier\": \"product\",\n+    \"tierMeaning\": \"product code\",\n+    \"declaredBudget\": 5,\n+    \"cap\": 5,\n+    \"criticality\": 55,\n+    \"artifactDeclaredAtRoundZero\": \"Exclude the required Visual Concept from the has-been-overridden signal (bugfix, Tier B)\",\n+    \"roundsSpent\": 3,\n+    \"pendingRequest\": false,\n+    \"ambiguous\": false,\n+    \"allowance\": 5,\n+    \"extensions\": []\n+  },\n+  \"rounds\": {\n+    \"completedReviewerPasses\": 3,\n+    \"byRound\": [\n+      {\n+        \"round\": 1,\n+        \"submitted_at\": \"2026-08-28T03:21:25Z\",\n+        \"source\": \"review\",\n+        \"findings\": 3\n+      },\n+      {\n+        \"round\": 2,\n+        \"submitted_at\": \"2026-08-28T03:36:25Z\",\n+        \"source\": \"review\",\n+        \"findings\": 3\n+      },\n+      {\n+        \"round\": 3,\n+        \"submitted_at\": \"2026-08-28T03:54:05Z\",\n+        \"source\": \"review\",\n+        \"findings\": 3\n+      }\n+    ],\n+    \"trend\": [\n+      3,\n+      3,\n+      3\n+    ],\n+    \"totalFindings\": 9\n+  },\n+  \"findings\": {\n+    \"unresolved\": 3,\n+    \"resolved\": 6,\n+    \"resolutionUnknown\": 0,\n+    \"note\": \"unresolved/resolved is THREAD state, not code state. An unresolved thread may already be fixed and awaiting a reply; isOutdated only tracks whether the anchored hunk moved. Treat these as 'what the reviewer has not yet been told is closed', never as 'what is still broken'.\",\n+    \"items\": [\n+      {\n+        \"threadId\": \"PRRT_kwDOR3LTYs6dDMgd\",\n+        \"path\": \"docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\",\n+        \"line\": 78,\n+        \"resolved\": true,\n+        \"outdated\": true,\n+        \"createdAt\": \"2026-08-28T03:21:25Z\",\n+        \"excerpt\": \"P1 Restore the fake visual override after step 5. This save permanently adds the example `a red hat` constraint to a real fact, while Setup declares no restoration is required.\"\n+      },\n+      {\n+        \"threadId\": \"PRRT_kwDOR3LTYs6dDMgf\",\n+        \"path\": \"docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\",\n+        \"line\": 100,\n+        \"resolved\": true,\n+        \"outdated\": true,\n+        \"createdAt\": \"2026-08-28T03:21:25Z\",\n+        \"excerpt\": \"P2 Remove the unrelated Facts-filter expectation. routes/admin.ts:1277 filters only on facts.enrichmentOverrides <> '{}', never on visualPromptStrategyOverride; the stated expectation is false.\"\n+      },\n+      {\n+        \"threadId\": \"PRRT_kwDOR3LTYs6dDMgg\",\n+        \"path\": \"docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\",\n+        \"line\": 106,\n+        \"resolved\": true,\n+        \"outdated\": true,\n+        \"createdAt\": \"2026-08-28T03:21:25Z\",\n+        \"excerpt\": \"P2 Avoid stranding a fact in a refresh cycle. Send back for refresh creates a candidate and pending review requiring human gates; Setup declares no restoration.\"\n+      },\n+      {\n+        \"threadId\": \"PRRT_kwDOR3LTYs6dDVGw\",\n+        \"path\": \"docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\",\n+        \"line\": 88,\n+        \"resolved\": true,\n+        \"outdated\": false,\n+        \"createdAt\": \"2026-08-28T03:36:25Z\",\n+        \"excerpt\": \"P2 Restore the captured override after the successful check. stampOverrideProvenance replaces updatedBy/updatedAt on each content change, so removing the scratch entry does not restore the captured object.\"\n+      },\n+      {\n+        \"threadId\": \"PRRT_kwDOR3LTYs6dDVGy\",\n+        \"path\": \"docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\",\n+        \"line\": 65,\n+        \"resolved\": true,\n+        \"outdated\": true,\n+        \"createdAt\": \"2026-08-28T03:36:25Z\",\n+        \"excerpt\": \"P2 Generate a fresh prompt before checking its contents. RuntimePromptPreview restores the prior result without recomputing; step 3 and R2 can pass against a pre-deployment prompt.\"\n+      },\n+      {\n+        \"threadId\": \"PRRT_kwDOR3LTYs6dDVG0\",\n+        \"path\": \"lib/api-zod/src/visualStrategyOverride.ts\",\n+        \"line\": 518,\n+        \"resolved\": true,\n+        \"outdated\": false,\n+        \"createdAt\": \"2026-08-28T03:36:25Z\",\n+        \"excerpt\": \"P2 Ignore descriptions attached to the default realization mode. subjectRealizationOverride {mode use_ai_plan, description ...} makes the predicate return true from the description although composeSubjectRealization returns empty for use_ai_plan.\"\n+      },\n+      {\n+        \"threadId\": \"PRRT_kwDOR3LTYs6dDbA1\",\n+        \"path\": \"docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\",\n+        \"line\": 88,\n+        \"resolved\": false,\n+        \"outdated\": false,\n+        \"createdAt\": \"2026-08-28T03:54:05Z\",\n+        \"excerpt\": \"P2 Split the two saves into separate UAT steps. uat-doc-format.md defines each ### heading as one driver-recorded step and requires one action per step, but this heading performs an add/save/assert followed by a remove/save/assert. If only one transition fails, /uat cannot record or resume the two results independently.\"\n+      },\n+      {\n+        \"threadId\": \"PRRT_kwDOR3LTYs6dDbA3\",\n+        \"path\": \"docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\",\n+        \"line\": 72,\n+        \"resolved\": false,\n+        \"outdated\": false,\n+        \"createdAt\": \"2026-08-28T03:54:05Z\",\n+        \"excerpt\": \"P2 Compare against the token-rendered Visual Concept. When the Visual Concept contains personalization tokens such as {NAME} or {SUBJ}, nanoBanana2 runs coreSceneOverride through renderIdentityTokens before the CORE SCENE reaches diagnostics, so the stated verbatim comparison is ambiguous or falsely failing.\"\n+      },\n+      {\n+        \"threadId\": \"PRRT_kwDOR3LTYs6dDbA5\",\n+        \"path\": \"docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\",\n+        \"line\": 92,\n+        \"resolved\": false,\n+        \"outdated\": false,\n+        \"createdAt\": \"2026-08-28T03:54:05Z\",\n+        \"excerpt\": \"P2 Reload the resolved summary before observing the badge. useFactEnrichmentEditing fetches overrideSummary only on mount; the whole-enrichment PATCH path merely adopts the returned enrichment, so EnrichmentEditor keeps rendering the original hasVisualStrategyOverride and the step reports a failure even when the new server predicate is correct.\"\n+      }\n+    ]\n+  },\n+  \"territory\": {\n+    \"inDiff\": 9,\n+    \"outsideDiff\": 0,\n+    \"unknown\": 0,\n+    \"outsideDiffPaths\": [],\n+    \"note\": \"Territory is mechanical (finding path vs. this PR's changed files). CAUSE (new-ground / propagation / wrong-fix / re-raised) is NOT derivable -- it has no machine-readable marker, and is deliberately left unclassified rather than guessed.\"\n+  },\n+  \"sinceLastReview\": {\n+    \"lastReviewedCommit\": \"26664466f89ef40b9ca0b797abe553ddab1b4fd8\",\n+    \"resolved\": true,\n+    \"since\": \"26664466f89ef40b9ca0b797abe553ddab1b4fd8\",\n+    \"head\": \"26664466f89ef40b9ca0b797abe553ddab1b4fd8\",\n+    \"commits\": 0,\n+    \"files\": [],\n+    \"patch\": \"\",\n+    \"behavioralFiles\": 0,\n+    \"proseOnly\": false,\n+    \"noChange\": true,\n+    \"note\": \"Branch movement since the last reviewed commit -- NOT the PR's diff. After a merge from the base branch this includes files already reviewed and merged there. Use `artifact` for the size of what is actually under review here.\"\n+  },\n+  \"provenance\": {\n+    \"githubVia\": \"mcp-snapshot (no bash transport reaches the GitHub API in this container)\",\n+    \"countingLogic\": \"scripts/review-counting.mjs\",\n+    \"caveat\": \"This record contains no narration from the loop it measures. If a field is unknown it says so; nothing here is inferred from the session's own account of its rounds.\"\n+  }\n+}\ndiff --git a/.agents/receipts/loop-budget-585.json b/.agents/receipts/loop-budget-585.json\nnew file mode 100644\nindex 0000000..ba92014\n--- /dev/null\n+++ b/.agents/receipts/loop-budget-585.json\n@@ -0,0 +1,8 @@\n+{\n+  \"pr\": 585,\n+  \"tier\": \"product\",\n+  \"budget\": 5,\n+  \"criticality\": 55,\n+  \"artifact\": \"Exclude the required Visual Concept from the has-been-overridden signal (bugfix, Tier B)\",\n+  \"declaredAt\": \"2026-08-28T03:13:05.464Z\"\n+}\ndiff --git a/artifacts/api-server/src/__tests__/enrichmentOverridesResolver.test.ts b/artifacts/api-server/src/__tests__/enrichmentOverridesResolver.test.ts\nindex bd7f7da..2f80df7 100644\n--- a/artifacts/api-server/src/__tests__/enrichmentOverridesResolver.test.ts\n+++ b/artifacts/api-server/src/__tests__/enrichmentOverridesResolver.test.ts\n@@ -104,11 +104,23 @@ describe(\"resolveEnrichment\", () => {\n       overrides: {},\n       visualPromptStrategyOverride: legacy,\n     });\n-    // The override still counts as renderable content — its core scene is set.\n-    assert.equal(summary.hasVisualStrategyOverride, true);\n+    // The core scene is preserved verbatim...\n     assert.equal(effective.visualPromptStrategyOverride?.coreSceneOverride, \"a giant lifting the earth\");\n     // ...and the full-schema parse fills the absent lists back in.\n     assert.deepEqual(effective.visualPromptStrategyOverride?.requiredVisualDetails, []);\n+    // ...but a core scene alone is the REQUIRED Visual Concept, not a moderator\n+    // override, so it does not raise the signal (#584). The #579 regression this\n+    // test exists for is the absence of a throw above, not this flag.\n+    assert.equal(summary.hasVisualStrategyOverride, false);\n+  });\n+\n+  // Regression — #584. The blob shape above is what EVERY production-ready fact\n+  // carries, so counting it made the signal true for the whole corpus.\n+  it(\"raises the signal for a legacy blob that also carries real override content\", () => {\n+    const legacy = { version: 1, coreSceneOverride: \"a giant lifting the earth\", requiredVisualDetails: [\"a red hat\"] } as unknown as\n+      FactEnrichment[\"visualPromptStrategyOverride\"];\n+    const { summary } = resolveEnrichment({ aiDerived: AI, overrides: {}, visualPromptStrategyOverride: legacy });\n+    assert.equal(summary.hasVisualStrategyOverride, true);\n   });\n \n   it(\"resolves a stored visual override that is a bare version stub\", () => {\n@@ -126,16 +138,35 @@ describe(\"resolveEnrichment\", () => {\n // is why the collector itself is guarded too. Imported from the api-server lib\n // rather than api-zod deliberately: this pins the caller, not the resolver.\n describe(\"serializeResolved — the caller that bypasses resolveEnrichment (#579)\", () => {\n-  it(\"does not crash on a legacy visual override when there is no AI baseline\", () => {\n-    const legacy = { version: 1, coreSceneOverride: \"{NAME} stands there confidently.\" } as unknown as\n-      FactEnrichment[\"visualPromptStrategyOverride\"];\n-    const out = serializeResolved({\n+  const serializeWith = (visual: unknown) =>\n+    serializeResolved({\n       aiDerived: null,\n       overrides: {},\n       effective: null,\n       enrichmentStatus: \"ready\",\n-      visualPromptStrategyOverride: legacy,\n+      visualPromptStrategyOverride: visual as FactEnrichment[\"visualPromptStrategyOverride\"],\n     }) as { overrideSummary: { hasVisualStrategyOverride: boolean } };\n+\n+  it(\"does not crash on a legacy visual override when there is no AI baseline\", () => {\n+    // The #579 regression is that this returns at all rather than throwing.\n+    const out = serializeWith({ version: 1, coreSceneOverride: \"{NAME} stands there confidently.\" });\n+    assert.equal(typeof out.overrideSummary.hasVisualStrategyOverride, \"boolean\");\n+  });\n+\n+  // Regression — #584, on the bypassing caller specifically: this path computes\n+  // the signal itself rather than reading the resolver's, so it needs its own\n+  // proof that a Visual-Concept-only blob does not raise it.\n+  it(\"does not raise the signal for a Visual-Concept-only blob (no AI baseline)\", () => {\n+    const out = serializeWith({ version: 1, coreSceneOverride: \"{NAME} stands there confidently.\" });\n+    assert.equal(out.overrideSummary.hasVisualStrategyOverride, false);\n+  });\n+\n+  it(\"still raises the signal when that same blob carries real override content\", () => {\n+    const out = serializeWith({\n+      version: 1,\n+      coreSceneOverride: \"{NAME} stands there confidently.\",\n+      bubbles: [{ type: \"speech\", entity: \"subject\", text: \"Not again.\" }],\n+    });\n     assert.equal(out.overrideSummary.hasVisualStrategyOverride, true);\n   });\n });\ndiff --git a/artifacts/api-server/src/__tests__/visualStrategyOverride.test.ts b/artifacts/api-server/src/__tests__/visualStrategyOverride.test.ts\nindex bc58766..07f6f01 100644\n--- a/artifacts/api-server/src/__tests__/visualStrategyOverride.test.ts\n+++ b/artifacts/api-server/src/__tests__/visualStrategyOverride.test.ts\n@@ -200,10 +200,43 @@ describe(\"token helpers\", () => {\n describe(\"hasRenderableVisualStrategyOverrideContent\", () => {\n   const asOv = (partial: Record<string, unknown>) => makeOverride(partial) as unknown as VisualPromptStrategyOverride;\n \n-  it(\"counts coreSceneOverride as renderable content\", () => {\n+  // `coreSceneOverride` is the storage for the REQUIRED Visual Concept, not a\n+  // moderator override: a fact cannot be saved or released to production with a\n+  // blank one (VisualConceptCard, and 8 required-gates across the admin/review\n+  // routes). So its presence says \"this fact reached concept review\", which is\n+  // universal — it can never distinguish a fact a moderator intervened on. It is\n+  // excluded from this signal while staying fully rendered/tokenized (#584).\n+  it(\"does NOT count coreSceneOverride — it is the required Visual Concept, not an override\", () => {\n     assert.equal(hasRenderableVisualStrategyOverrideContent(asOv({})), false);\n     assert.equal(hasRenderableVisualStrategyOverrideContent(asOv({ coreSceneOverride: \"  \" })), false);\n-    assert.equal(hasRenderableVisualStrategyOverrideContent(asOv({ coreSceneOverride: \"{NAME} on a throne\" })), true);\n+    assert.equal(\n+      hasRenderableVisualStrategyOverrideContent(asOv({ coreSceneOverride: \"{NAME} on a throne\" })),\n+      false,\n+    );\n+    // The exact production blob from review #6880 / fact 22587 (#584).\n+    assert.equal(\n+      hasRenderableVisualStrategyOverrideContent(\n+        { version: 1, coreSceneOverride: \"{NAME} stands there confidently.\" } as unknown as VisualPromptStrategyOverride,\n+      ),\n+      false,\n+    );\n+  });\n+\n+  it(\"still counts every other rendered field when a Visual Concept is also present\", () => {\n+    // The negative case that matters: a core scene must not MASK real override\n+    // content, and each field must count independently of it.\n+    const withScene = (partial: Record<string, unknown>) =>\n+      hasRenderableVisualStrategyOverrideContent(\n+        asOv({ coreSceneOverride: \"{NAME} stands there confidently.\", ...partial }),\n+      );\n+    assert.equal(withScene({ requiredVisualDetails: [\"a red hat\"] }), true);\n+    assert.equal(withScene({ forbiddenVisualDetails: [\"no logos\"] }), true);\n+    assert.equal(withScene({ compositionGuidance: [\"wide shot\"] }), true);\n+    assert.equal(withScene({ styleAgnosticPromptAdditions: [\"grainy\"] }), true);\n+    assert.equal(withScene({ negativePromptAdditions: [\"blurry\"] }), true);\n+    assert.equal(withScene({ roleBindings: [{ entity: \"the bartender\", visualRole: \"pouring\" }] }), true);\n+    assert.equal(withScene({ violencePolicyOverride: { mode: \"soften\", intensity: \"mild\" } }), true);\n+    assert.equal(withScene({ subjectRealizationOverride: { mode: \"subject_as_object\", description: \"\" } }), true);\n   });\n \n   it(\"ignores admin-only fields (moderatorIntent, notesForModerator)\", () => {\n@@ -213,6 +246,52 @@ describe(\"hasRenderableVisualStrategyOverrideContent\", () => {\n     );\n   });\n \n+  // Guards the scoping of the #584 fix: excluding the Visual Concept from the\n+  // \"was this overridden\" SIGNAL must not remove it from the RENDER/TOKENIZE\n+  // path, which is a different question answered by a different function.\n+  it(\"leaves coreSceneOverride fully rendered and tokenized (signal ≠ render path)\", () => {\n+    const ov = asOv({ coreSceneOverride: \"{NAME} on a throne\" });\n+    const entry = collectRenderedTextEntries(ov).find((e) => e.path === \"coreSceneOverride\");\n+    assert.ok(entry, \"coreSceneOverride must still be emitted as rendered text\");\n+    assert.equal(entry!.value, \"{NAME} on a throne\");\n+    assert.equal(entry!.kind, \"prose\");\n+    assert.equal(isVisualStrategyRenderedTextPath(\"coreSceneOverride\"), true);\n+    // Token validation on the Visual Concept must still fire.\n+    assert.ok(firstOverrideTokenError(asOv({ coreSceneOverride: \"{NOPE} on a throne\" })));\n+    assert.equal(firstOverrideTokenError(ov), null);\n+  });\n+\n+  // Regression — #584 round 2. Same class as the coreSceneOverride bug: a field\n+  // counted as override evidence that the compiler does not actually render.\n+  // `composeSubjectRealization` (nanoBanana2.ts:511) returns \"\" whenever the mode\n+  // is `use_ai_plan`, whatever the description says — so a description parked\n+  // under the default mode is a no-op that must not raise the signal.\n+  it(\"does NOT count a subjectRealizationOverride description under the default mode\", () => {\n+    assert.equal(\n+      hasRenderableVisualStrategyOverrideContent(\n+        asOv({ subjectRealizationOverride: { mode: \"use_ai_plan\", description: \"a stern librarian\" } }),\n+      ),\n+      false,\n+    );\n+    // ...and it still counts the moment the mode is anything else, description or not.\n+    assert.equal(\n+      hasRenderableVisualStrategyOverrideContent(\n+        asOv({ subjectRealizationOverride: { mode: \"subject_as_object\", description: \"a stern librarian\" } }),\n+      ),\n+      true,\n+    );\n+    // A real override elsewhere is not masked by the inert realization block.\n+    assert.equal(\n+      hasRenderableVisualStrategyOverrideContent(\n+        asOv({\n+          subjectRealizationOverride: { mode: \"use_ai_plan\", description: \"a stern librarian\" },\n+          requiredVisualDetails: [\"a red hat\"],\n+        }),\n+      ),\n+      true,\n+    );\n+  });\n+\n   it(\"counts policy overrides and non-default subject realization\", () => {\n     assert.equal(\n       hasRenderableVisualStrategyOverrideContent(asOv({ violencePolicyOverride: { mode: \"soften\", intensity: \"mild\" } })),\ndiff --git a/docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md b/docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\nnew file mode 100644\nindex 0000000..8815d54\n--- /dev/null\n+++ b/docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md\n@@ -0,0 +1,160 @@\n+# PR #585 — The \"Overridden\" badge only shows when someone actually overrode something — UAT\n+\n+**Workstream:** #584\n+\n+The admin enrichment editor was telling you a fact had a moderator override\n+when it had none. The badge keyed off the Visual Concept — the \"describe the\n+picture\" text — which every fact is *required* to have. So it lit up on\n+essentially every fact that had reached concept review, which is most of them,\n+and the one signal meant to say \"a human touched this one\" said it about\n+everything.\n+\n+You found this yourself during PR #582's UAT: review #6880 showed\n+**\"Overridden: Visual Strategy\"** while its entire Visual Strategy Override\n+panel was empty. Your call was that the Visual Concept should be omitted from\n+the check, and that is exactly what shipped.\n+\n+**The risk this run is really checking** is the other direction. The Visual\n+Concept is still sent to the image engine and still has its `{NAME}` tokens\n+validated — it just no longer counts as *evidence of an override*. Those are\n+two different jobs for one field, and the way this fix could have gone wrong is\n+by removing the field from both. **Steps 3 and 4 are the load-bearing ones**:\n+they confirm the picture-prompt still carries the scene and tokens are still\n+checked. Steps 1 and 2 confirm the badge itself.\n+\n+Steps 1–4 and R1–R2 are read-only. **Steps 5 and 6 are the only ones that\n+write**, and they are a pair: 5 adds a scratch override, 6 removes it again.\n+\n+Two things about this screen that would otherwise read as bugs, so they are\n+built into the steps rather than left to trip you up. **The badge is computed\n+when the editor loads, not after a save** — so every step that saves tells you\n+to reopen the editor before reading it. And **the compiled prompt substitutes a\n+sample name for the tokens** — so `{NAME} stands there confidently.` appears in\n+the prompt as `David stands there confidently.`, and you are comparing the\n+scene, not the literal characters.\n+\n+## Setup\n+\n+- [claude] Confirm `main` is synced to the Repl and the checked-out SHA matches the merge commit, before anything is read.\n+- [claude] Capture the count of facts carrying a Visual Concept — the size of the set that was showing the incorrect badge — so \"it's fixed\" has a number behind it.\n+- [claude] Identify and name in the preview: one fact whose override panel is genuinely empty (review #6880 is the known case), and one with real override content. Steps 1, 2, 5, 6 and R2 each need a known subject rather than a hunt.\n+- [claude] Before step 5 runs, capture the current stored `visualPromptStrategyOverride` for the fact step 5 will edit — including its `updatedBy` and `updatedAt` — and record it in the run record, so the restore below has a real captured value rather than a guess.\n+- [david] Sign in to the admin console as yourself; every step is an admin screen.\n+- [restore] The visual override on the fact edited in steps 5 and 6 — restore it to the value captured before step 5, **after every run, not only an interrupted one**. Removing the scratch entry in step 6 restores the *content* but not the provenance: each save that changes content re-stamps `updatedBy`/`updatedAt`, so without this the fact is left recorded as last edited by the tester at test time.\n+\n+## Steps\n+\n+### 1. The badge is gone where nothing was overridden\n+\n+**Do:** Go to **Admin → Moderation** and open the review I name in the preview\n+(the fact with an empty override panel — review #6880 unless I say otherwise).\n+Expand **Advanced Options** and look at the **AI Visual Classification** block,\n+just under the amber \"Ambiguous sentence-initial entity\" note.\n+\n+**Expect:** There is **no** orange `Overridden: Visual Strategy` bar. The\n+classification fields below it (Joke Mechanism, Depiction Style, Overhype Fit\n+and so on) are unchanged and still populated.\n+\n+### 2. The badge still appears where something really was overridden\n+\n+**Do:** Open the review I name in the preview as having real override content —\n+a required visual detail, a speech bubble, or a composition note. Expand\n+**Advanced Options** and look at the same **AI Visual Classification** block.\n+\n+**Expect:** The orange `Overridden: Visual Strategy` bar **is** shown. This is\n+the case that must not have been broken by the fix.\n+\n+### 3. The Visual Concept still reaches the picture prompt\n+\n+**Do:** On the same review as step 2, read the text in the **Visual Concept —\n+describe the picture** field, then open **Prompt Diagnostics** in the same\n+Advanced Options section. **Click \"Generate runtime prompt preview\" and wait for\n+it to finish**, then read the compiled prompt. Do not read whatever is already\n+on screen when the panel opens — the panel restores the last saved result for\n+this fact without recomputing, which on a browser that has opened it before\n+would be a prompt built by the *old* code. (Generating is read-only: the panel\n+computes a preview and does not render or save.)\n+\n+**Expect:** The compiled prompt contains a **CORE SCENE** section describing the\n+same scene as the Visual Concept you just read. The Visual Concept is still\n+reaching the engine — which is what the fix had to avoid breaking.\n+\n+**Not a mismatch:** any `{NAME}` / `{SUBJ}` / `{POSS}` tokens in the field are\n+substituted for the sample identity shown in the Prompt Diagnostics controls\n+before the scene reaches the prompt. So a concept reading `{NAME} stands there\n+confidently.` appears as `David stands there confidently.` (or whatever sample\n+name is set). Match the scene, not the literal characters.\n+\n+### 4. A bad token in the Visual Concept is still caught\n+\n+**Do:** In the **Visual Concept — describe the picture** field on any fact, type\n+`{NOPE}` at the end of the existing text. **Do not save.**\n+\n+**Expect:** A token warning appears under the field flagging the unknown token,\n+the same validation as before this PR. **Then delete the `{NOPE}` you typed**,\n+leaving the field exactly as you found it, and close the editor without saving.\n+\n+### 5. Saving a real override raises the badge\n+\n+**Do:** Open the fact I name in the preview as having an empty override panel.\n+In **Advanced Options → Visual Strategy Override**, add one **Required visual\n+detail** with the text `uat-585-scratch`, and save. **Then close the editor and\n+reopen it** — the badge is computed when the editor loads, so reading it without\n+reopening shows the value from before your save.\n+\n+**Expect:** On reopening, the orange `Overridden: Visual Strategy` bar\n+**appears** — the badge tracks the override you just made. Leave the entry in\n+place; step 6 removes it.\n+\n+### 6. Clearing that override lowers the badge again\n+\n+**Do:** In the same fact's **Visual Strategy Override** panel, remove the\n+`uat-585-scratch` required visual detail you added in step 5, and save. **Close\n+the editor and reopen it** again, for the same reason.\n+\n+**Expect:** On reopening, the `Overridden: Visual Strategy` bar is **gone**. The\n+distinctive text makes the entry unmistakable if anything is left behind — tell\n+me if this save does not clear it, and I will restore from the value captured in\n+setup.\n+\n+## Regression\n+\n+### R1. The enrichment editor still opens on a normal fact\n+\n+**Do:** Open **Admin → Moderation**, pick any fact with completed enrichment,\n+and open its enrichment editor.\n+\n+**Expect:** The editor loads with its classification fields populated —\n+archetype, subtype, fit — and no error banner.\n+\n+### R2. Real override content still reaches the prompt alongside the scene\n+\n+**Do:** On the fact from step 2 (the one with real override content), open\n+**Prompt Diagnostics**, **click \"Generate runtime prompt preview\" and wait for\n+it to finish** — same reason as step 3, a restored result may predate the merge\n+— then read the compiled prompt.\n+\n+**Expect:** The prompt carries **both** the CORE SCENE **and** that fact's\n+override content — its required detail, bubble, or composition note. This is\n+the \"a core scene must not mask real override content\" invariant seen end to\n+end rather than in a unit test. As in step 3, tokens are substituted for the\n+sample identity, so match the scene rather than the literal characters.\n+\n+## Not bugs\n+\n+- **A fact whose override panel is empty shows no badge even though its Visual\n+  Concept is long and detailed.** That is the entire point of the fix: the\n+  Visual Concept is required on every fact, so it can never distinguish one\n+  fact from another. Only the fields under **Visual Strategy Override** count.\n+- **The Facts page \"Overridden\" filter is unchanged by this PR.** It filters on\n+  the taxonomy override map (`enrichmentOverrides`), which is a different thing\n+  from the visual override and is not touched here — so its result set should\n+  look exactly as it did before. An earlier draft of this doc claimed the list\n+  would shrink; that was wrong and there is nothing to check there.\n+- **There is no way to tell an AI-drafted Visual Concept from one you wrote\n+  yourself.** The field stores both identically and nothing records which it\n+  was, which is *why* it is excluded rather than refined. If distinguishing\n+  them is worth having, that is a product decision and a separate piece of\n+  work, not a defect here.\n+- **A recovered enrichment job still shows its old error text on Queue\n+  Health.** Unrelated to this PR — a known observability gap recorded on #579.\ndiff --git a/lib/api-zod/src/visualStrategyOverride.ts b/lib/api-zod/src/visualStrategyOverride.ts\nindex 78355ac..8481cf7 100644\n--- a/lib/api-zod/src/visualStrategyOverride.ts\n+++ b/lib/api-zod/src/visualStrategyOverride.ts\n@@ -465,18 +465,57 @@ export const visualPromptStrategyOverrideSchema = visualPromptStrategyOverrideBa\n     });\n   });\n \n+/** Rendered-text paths that are NOT evidence of moderator intervention for THIS\n+ *  override, and so are excluded from `hasRenderableVisualStrategyOverrideContent`\n+ *  while staying fully rendered and tokenized everywhere else.\n+ *\n+ *  Two reasons a path lands here, and they are different:\n+ *\n+ *  1. **Required of every fact.** `coreSceneOverride` holds the Visual Concept,\n+ *     which a fact cannot be saved or released to production without (see\n+ *     `VisualConceptCard`, and the required-gates in the admin/review routes).\n+ *     Every production-ready fact carries one, so its presence says \"this fact\n+ *     reached concept review\", never \"a moderator overrode something\" (#584). It\n+ *     is also not a discriminator we could refine: the field holds the accepted\n+ *     AI draft and a hand-written scene identically, with nothing stored to tell\n+ *     them apart.\n+ *  2. **Inert in this state.** A `subjectRealizationOverride.description` under\n+ *     `mode: \"use_ai_plan\"` renders nothing — the compiler's\n+ *     `composeSubjectRealization` returns \"\" for that mode whatever the\n+ *     description says — so it is a no-op, not an override. (A non-default mode\n+ *     is caught by the explicit check in the predicate below, description or\n+ *     not.)\n+ *\n+ *  Both are the same underlying rule: this signal counts only content that both\n+ *  reaches the engine AND represents a human decision. */\n+function nonOverrideRenderedPaths(ov: VisualPromptStrategyOverride): ReadonlySet<string> {\n+  const paths = new Set<string>([\"coreSceneOverride\"]);\n+  if (!ov.subjectRealizationOverride || ov.subjectRealizationOverride.mode === \"use_ai_plan\") {\n+    paths.add(\"subjectRealizationOverride.description\");\n+  }\n+  return paths;\n+}\n+\n /**\n  * True when the override carries any content that is RENDERED into the engine\n- * prompt. The single source of truth for \"enabled but empty\" style checks so\n- * UI surfaces can't drift on what counts as content. Admin-only fields\n- * (moderatorIntent, notesForModerator) deliberately do NOT count.\n+ * prompt **and** indicates a deliberate moderator override. The single source of\n+ * truth for \"enabled but empty\" style checks so UI surfaces can't drift on what\n+ * counts as content. Admin-only fields (moderatorIntent, notesForModerator)\n+ * deliberately do NOT count, and neither do `nonOverrideRenderedPaths` above.\n+ *\n+ * This is the \"was this overridden\" SIGNAL, a different question from \"what gets\n+ * rendered\" — which stays `collectRenderedTextEntries`, deliberately untouched\n+ * by the exclusions above so tokenization and prompt compilation are unaffected.\n  */\n export function hasRenderableVisualStrategyOverrideContent(\n   ov: VisualPromptStrategyOverride,\n ): boolean {\n   if (ov.supportingTextPolicyOverride || ov.violencePolicyOverride) return true;\n   if (ov.subjectRealizationOverride && ov.subjectRealizationOverride.mode !== \"use_ai_plan\") return true;\n-  return collectRenderedTextEntries(ov).some(({ value }) => value.trim().length > 0);\n+  const excluded = nonOverrideRenderedPaths(ov);\n+  return collectRenderedTextEntries(ov).some(\n+    ({ path, value }) => !excluded.has(path) && value.trim().length > 0,\n+  );\n }\n \n /** An empty override scaffold (all lists empty). Under presence-based activation",
    "patchNote": "base...head: the artifact this round's findings are about. This is the diff to read when the rubric asks whether a finding describes a critical flaw. `sinceLastReview.patch` is separate and is normally empty under the write-gate rule, since the judge rules on an already-reviewed head."
  },
  "budget": {
    "tier": "product",
    "tierMeaning": "product code",
    "declaredBudget": 5,
    "cap": 5,
    "criticality": 55,
    "artifactDeclaredAtRoundZero": "Exclude the required Visual Concept from the has-been-overridden signal (bugfix, Tier B)",
    "roundsSpent": 4,
    "pendingRequest": false,
    "ambiguous": false,
    "allowance": 5,
    "extensions": []
  },
  "rounds": {
    "completedReviewerPasses": 4,
    "byRound": [
      {
        "round": 1,
        "submitted_at": "2026-08-28T03:21:25Z",
        "source": "review",
        "findings": 3
      },
      {
        "round": 2,
        "submitted_at": "2026-08-28T03:36:25Z",
        "source": "review",
        "findings": 3
      },
      {
        "round": 3,
        "submitted_at": "2026-08-28T03:54:05Z",
        "source": "review",
        "findings": 3
      },
      {
        "round": 4,
        "submitted_at": "2026-08-28T04:11:51Z",
        "source": "review",
        "findings": 2
      }
    ],
    "trend": [
      3,
      3,
      3,
      2
    ],
    "totalFindings": 11
  },
  "findings": {
    "unresolved": 2,
    "resolved": 9,
    "resolutionUnknown": 0,
    "note": "unresolved/resolved is THREAD state, not code state. An unresolved thread may already be fixed and awaiting a reply; isOutdated only tracks whether the anchored hunk moved. Treat these as 'what the reviewer has not yet been told is closed', never as 'what is still broken'.",
    "items": [
      {
        "threadId": "PRRT_kwDOR3LTYs6dDMgd",
        "path": "docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md",
        "line": 78,
        "resolved": true,
        "outdated": true,
        "createdAt": "2026-08-28T03:21:25Z",
        "excerpt": "P1 Restore the fake visual override after step 5 — the save permanently adds a constraint to a real fact while Setup declares no restoration."
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDMgf",
        "path": "docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md",
        "line": 100,
        "resolved": true,
        "outdated": true,
        "createdAt": "2026-08-28T03:21:25Z",
        "excerpt": "P2 Remove the unrelated Facts-filter expectation — admin.ts:1277 filters only on enrichmentOverrides, so the stated expectation is false."
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDMgg",
        "path": "docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md",
        "line": 106,
        "resolved": true,
        "outdated": true,
        "createdAt": "2026-08-28T03:21:25Z",
        "excerpt": "P2 Avoid stranding a fact in a refresh cycle — send-back opens a review only a human gate closes."
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDVGw",
        "path": "docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md",
        "line": 88,
        "resolved": true,
        "outdated": false,
        "createdAt": "2026-08-28T03:36:25Z",
        "excerpt": "P2 Restore the captured override after the successful check — stampOverrideProvenance re-stamps updatedBy/updatedAt on each content change."
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDVGy",
        "path": "docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md",
        "line": 65,
        "resolved": true,
        "outdated": true,
        "createdAt": "2026-08-28T03:36:25Z",
        "excerpt": "P2 Generate a fresh prompt before checking its contents — RuntimePromptPreview restores the prior result without recomputing."
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDVG0",
        "path": "lib/api-zod/src/visualStrategyOverride.ts",
        "line": 518,
        "resolved": true,
        "outdated": false,
        "createdAt": "2026-08-28T03:36:25Z",
        "excerpt": "P2 Ignore descriptions attached to the default realization mode — composeSubjectRealization returns empty for use_ai_plan yet the predicate returns true from the description."
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDewk",
        "path": "docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md",
        "line": 92,
        "resolved": true,
        "outdated": true,
        "createdAt": "2026-08-28T03:54:05Z",
        "excerpt": "P2 Reload the resolved summary before observing the badge — useFactEnrichmentEditing fetches overrideSummary only on mount."
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDewo",
        "path": "docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md",
        "line": 88,
        "resolved": true,
        "outdated": true,
        "createdAt": "2026-08-28T03:54:05Z",
        "excerpt": "P2 Split the two saves into separate UAT steps — uat-doc-format.md requires one action per step."
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDewu",
        "path": "docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md",
        "line": 72,
        "resolved": true,
        "outdated": true,
        "createdAt": "2026-08-28T03:54:05Z",
        "excerpt": "P2 Compare against the token-rendered Visual Concept — nanoBanana2 runs coreSceneOverride through renderIdentityTokens before diagnostics."
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDpVY",
        "path": "lib/api-zod/src/visualStrategyOverride.ts",
        "line": 517,
        "resolved": false,
        "outdated": false,
        "createdAt": "2026-08-28T04:11:51Z",
        "excerpt": "P2 Ignore incomplete compound rows when raising the badge. When an admin adds a bubble and saves before entering its text, BubbleEditor persists { entity: \"subject\", text: \"\" }; this predicate sees the non-empty entity from collectRenderedTextEntries and raises the Visual Strategy badge, while nanoBanana2.ts:1281 filters that row out because both entity and text are required. Partially completed r"
      },
      {
        "threadId": "PRRT_kwDOR3LTYs6dDpVd",
        "path": "docs/tests/UAT/PR585_VISUAL_CONCEPT_NOT_AN_OVERRIDE_UAT.md",
        "line": 39,
        "resolved": false,
        "outdated": false,
        "createdAt": "2026-08-28T04:11:51Z",
        "excerpt": "P2 Count only facts whose badge was actually incorrect. Counting every fact with a Visual Concept overstates the affected set: the real-override fact also carries a required Visual Concept but its badge was legitimate, and blank or whitespace-only concepts did not satisfy the old trimmed-content predicate either. Compute the count using the actual before/after condition — old predicate true and ne"
      }
    ]
  },
  "territory": {
    "inDiff": 11,
    "outsideDiff": 0,
    "unknown": 0,
    "outsideDiffPaths": [],
    "note": "Territory is mechanical (finding path vs. this PR's changed files). CAUSE (new-ground / propagation / wrong-fix / re-raised) is NOT derivable -- it has no machine-readable marker, and is deliberately left unclassified rather than guessed."
  },
  "sinceLastReview": {
    "lastReviewedCommit": "96697acbe241d406a3d18d9b3c1f6a3f9d33615f",
    "resolved": true,
    "since": "96697acbe241d406a3d18d9b3c1f6a3f9d33615f",
    "head": "96697acbe241d406a3d18d9b3c1f6a3f9d33615f",
    "commits": 0,
    "files": [],
    "patch": "",
    "behavioralFiles": 0,
    "proseOnly": false,
    "noChange": true,
    "note": "Branch movement since the last reviewed commit -- NOT the PR's diff. After a merge from the base branch this includes files already reviewed and merged there. Use `artifact` for the size of what is actually under review here."
  },
  "provenance": {
    "githubVia": "mcp-snapshot (no bash transport reaches the GitHub API in this container)",
    "countingLogic": "scripts/review-counting.mjs",
    "caveat": "This record contains no narration from the loop it measures. If a field is unknown it says so; nothing here is inferred from the session's own account of its rounds."
  }
}
