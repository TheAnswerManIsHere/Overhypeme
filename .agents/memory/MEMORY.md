- [Auth middleware Bearer-cookie priority](auth-bearer-cookie-fallback.md) — stale Bearer in localStorage blocked valid cookie sessions; auth middleware now falls back to cookie when Bearer is stale.
- [OpenAI no Replit proxy](openai-no-replit-proxy.md) — all OpenAI/embeddings calls use direct OPENAI_API_KEY; never add an AI_INTEGRATIONS_OPENAI_* proxy fallback.
- [api-zod barrel codegen](api-zod-barrel-codegen.md) — lib/api-zod/src/index.ts is regenerated by patch-generated.mjs on codegen/pretest; add barrel exports to its array, not the file. Composite dist/src can skew.
- [Test database isolation](test-db-isolation.md) — PR132 runner uses per-DB clones; requires TEST_DATABASE_URL (heliumdb_test); both pretest and test-db.sh use it automatically when set.
- [Image-prompt preview parity](image-prompt-preview-parity.md) — Fact-page preview + engine workbench + production all share generateImagePromptPlan+compileForSubjectRenderMode; previews must share PREVIEW_SUBJECT_NAME; residual prose diff is temp 0.4, not caching.
- [Test idle-drain timeout](test-idle-drain-timeout.md) — single-file test runs hung ~60s on the pg pool's idle timer, not sharding; lib/db now auto-detects the test runner to unref idle handles.
- [Running long test suites](running-long-test-suites.md) — foreground `pnpm test | tail` gets killed (exit -1, no output); run node suites detached+poll in one call, run vitest via the sentry-tests workflow.
- [Canvas reload-loop diagnosis](canvas-reload-loop-diagnosis.md) — ~1.3s vite connecting/connected pairs = crashed-iframe auto-reloads; also duplicate Express routes shadow silently — curl the live server, compare error bodies.
- [Restart the branch before exploring, not just before pushing](explore-after-merge-restart-branch-first.md) — after a prior PR on the same branch squash-merges, exploring/planning before restarting onto origin/main can silently read a stale local tree and produce a confidently-wrong plan.
- [React setState updater not synchronous](react-setstate-updater-not-synchronous.md) — writing to a ref inside a `setState(prev => …)` updater doesn't fix a same-tick stale-read race; resolve against the ref and write it BEFORE calling the setter instead.
- [Zod nested superRefine path notation](zod-nested-superrefine-path-notation.md) — a flattened `validateEnrichment`-style error path joins an array index as `.0.` (dots), not `[0]` (brackets); verify empirically before writing a path-matching regex against it.
- [Run a route's existing security regression suite when you touch it](run-security-regression-suite-for-touched-routes.md) — a security test named for the finding (e.g. localAuth.security) can assert a route's exact response; changing even the error *message* breaks it. Grep src/__tests__ for the route path and run every hit, not just the nearby suite.
- [Dependabot pnpm workspace needs `directories` globs](dependabot-pnpm-workspace-directories.md) — `directory: "/"` covers only the root manifest; use `directories` mirroring pnpm-workspace.yaml (/artifacts/*, /cloudflare/*, /lib/*, /scripts) or most workspace deps go unmonitored.
- [Env knob split must preserve legacy default](env-knob-split-preserve-legacy-default.md) — splitting one shared config env var into several must default each new one to the OLD knob's resolved value, not a fresh literal, or a deploy's tuned rate-limit cap silently vanishes on upgrade.
- [Sentry API needs a network allowlist, not just a token](sentry-api-requires-network-allowlist.md) — a valid SENTRY_AUTH_TOKEN still 403s if the Claude Code environment's egress policy blocks sentry.io; check the proxy status endpoint's recentRelayFailures to tell a policy denial (CONNECT tunnel 403) from a token-scope 403 (normal response body) before assuming the credential is wrong.
- [Raw-SQL migration additions need a schema.ts shadow](raw-sql-migration-needs-schema-shadow.md) — a migration's hand-written CHECK constraint, CREATE INDEX, or standalone CREATE SEQUENCE with no matching schema.ts declaration is invisible to `drizzle-kit push`, which can silently omit it or (worse) drop one `migrate` already added; add the matching `index()`/`check()` (on the owning `pgTable`) or `pgSequence()` in the same commit.
- [Repo-wide typecheck vs per-package tsc -b](repo-wide-typecheck-vs-per-package.md) — a new cross-package `@workspace/*` import needs the consuming package's tsconfig.json project reference updated; per-package `tsc -b` on the packages you're editing won't catch a sibling package (e.g. `scripts`) left unreferenced — run the repo-wide `pnpm typecheck`.
- [db.transaction guard: avoid never-narrowing](transaction-guard-never-narrowing.md) — a `let` mutated inside `db.transaction(async (tx) => {...})` narrows to `never` when read after the `await`; return a discriminated-union result from the callback instead of mutating a captured variable.
- [pnpm override scope and application](pnpm-override-scope-and-application.md) — a bare `overrides:` entry can silently no-op via `pnpm install`/`--force` (check the lockfile's echoed `overrides:` block; `pnpm update <pkg> -r` is what actually applies it); a workspace-wide override also doesn't lock a package's own direct dependency specifier for that same package.
- [Dependabot Alerts API is unavailable in this environment](dependabot-alerts-api-unavailable.md) — no GitHub MCP tool covers it, and a direct REST call via the session's own token gets a deliberate proxy 403 ("GitHub access is not enabled"); ask the user for Dependabot UI screenshots instead.
- [DB schema-push command blocked by the sandbox classifier](db-command-blocked-by-sandbox-classifier.md) — `pnpm --filter @workspace/db push-force` was denied even with a working test DB; lean on a sibling PR's already-green CI against the same dependency versions instead of fighting the block.
- [CodeQL false-flags hand-rolled rate-limit/CSRF controls](codeql-missing-rate-limiting-csrf-false-positive.md) — `js/missing-rate-limiting` and `js/missing-token-validation` only recognize known npm packages, not `checkSharedRateLimit` or the double-submit CSRF cookie; investigate before adding redundant code to satisfy the scanner.
- [Never cite a docs/plans/ path from code](plan-doc-path-never-cite-from-code.md) — plan-review branches are never merged, so a `docs/plans/PLAN_*.md` citation in a docstring is dangling from the moment it's written; cite `deferred-work.md`/`decisions.md` instead.
- [Stripe account identity in keys and object IDs](stripe-account-identity-in-keys-and-object-ids.md) — API keys and object IDs (`price_`, `prod_`, ...) share an account-identifying substring; match it to confirm/deny "same Stripe account" without an API call. A Sandbox is a separate account, not a mode.
- [Git commit message backticks trigger shell substitution](git-commit-message-backtick-shell-substitution.md) — a `-m "..."` string with backtick-quoted code spans gets executed by bash before git sees it, corrupting an already-pushed commit; write the message to a file and use `git commit -F` instead.
- [GitHub MCP pull_request_read shape differs from REST](github-mcp-review-comments-shape-differs-from-rest.md) — get_review_comments groups by thread with a bare `author` string, no numeric id, no in_reply_to_id, no pull_request_review_id, and an inconsistent `[bot]` suffix vs get_reviews; verify against a real call before mapping to REST-shaped code.
- [A /document harvest branch can hit a real merge conflict against another concurrent docs PR](document-ceremony-concurrent-docs-pr-conflict.md) — restarting onto origin/main fixes staleness, not collisions; if another PR merged the same shared docs files (decisions.md, MEMORY.md, etc.) in the interval, resolve by keeping both sides' entries, never picking one.
- [A CI guard that skips on missing inputs is indistinguishable from one that verified everything, by CI status alone](ci-guard-must-fail-loud-on-missing-inputs.md) — a coverage check that returns 0 both when it ran and when it silently skipped for lack of credentials proves nothing on a green CI check (its stdout does differ, but nobody reads it without cause); make it throw when required inputs are missing in an environment where they should always be present (e.g. `pull_request` CI), not just skip.
- [A stale `lib/db/dist` can make tsc show phantom schema errors after a merge](lib-db-dist-stale-after-schema-merge.md) — `git merge` never touches the gitignored `dist/` that `tsc -b`'s project references actually resolve through; a merge bringing in new schema exports can report `TS2305: no exported member` for an export plainly present in source until `npx tsc -b lib/db --force` rebuilds it (a bare `tsc -b` can no-op; deleting `dist/` outright trades one error for a worse one).
- [Moving a section between CLAUDE.md and a skill leaves prose cross-references stale and invisible to the link checker](prose-cross-refs-invisible-to-link-checker.md) — check-docs-accuracy.mjs only validates markdown `[text](path)` links; a prose sentence naming a heading ("see CLAUDE.md's *X* section") passes clean even after that heading moves. Grep the whole repo for the old heading text after any migration, don't wait for review to find each stale reference one at a time.
- [A module-level default-exported singleton evaluates on ANY import, under `--test-isolation=none`](app-ts-eager-singleton-test-isolation.md) — importing only a named export still runs the whole module's top-level code, including an eager `const app = createApp(); export default app;`; that baked in one test file's env state and leaked into another sharing the same process. Never memoize a singleton that captures mutable env/config at import time in a file tests import — call the factory fresh every time. (Not a ban on module-level state generally: a shared DB pool or an empty lazy cache variable, with no per-test env dependency baked in at construction, is fine.)
- [GitHub Project field/option names need normalized matching](github-project-field-names-need-normalized-matching.md) — a hand-typed UI name (a field or an option) isn't a stable identifier; compare it normalized (case/emoji/hyphen-insensitive), never exact-string. The real `Waiting On` field (capital O) vs. a hardcoded `Waiting on` failed every workstream sync on the first live run.
- [A brand-new workflow can't be workflow_dispatch-triggered until it's on the default branch](workflow-dispatch-needs-default-branch.md) — GitHub registers dispatchable workflows from the default branch only, even though `workflow_dispatch`'s own `ref` param looks like it should target any branch with the file; a pre-merge dispatch against the feature branch 404s.
- [Writing a GitHub Action that mutates issue labels concurrently with agents/humans](github-actions-concurrent-label-mutation-pattern.md) — add-then-clean (never delete-then-add), revalidate as a subset check right before mutating, derive "stale" as the intersection of a before/after snapshot pair (never from the after read alone), and re-derive a retry's target from scratch rather than trusting the dispatch-time read. Took ~12 review rounds to converge.
- [Skipping CI on some PRs: job-level `if:`, never workflow-level `paths:`](github-actions-required-checks-job-if-vs-paths-filter.md) — a `paths`-filtered workflow that never triggers leaves a required check stuck at "Expected" forever; a job skipped via `if:` reports "Success" instead. A `needs:`-gated job also SKIPS (and thus reports success) if its dependency fails, so the gate must explicitly run on failure too, not just on a positive skip signal.
- [`concurrency` groups: `github.ref` collides across pushes; `queue: max` can't combine with a conditional `cancel-in-progress`](github-actions-concurrency-group-key-and-queue-max.md) — every push to the same branch shares one `github.ref`, so using it as a group-key fallback still lets GitHub's single-pending-slot default silently drop a queued run; fall back to `github.run_id` (unique per run) instead. `queue: max` is a hard validation error if `cancel-in-progress` can ever evaluate `true` in that same block.
- [GitHub Actions runs silently not starting can be a status.github.com outage, not a quota/billing problem](github-actions-outage-mimics-quota.md) — a public repo's Actions minutes are free/unmetered (a billing page showing a dollar figure near the included cap can still read $0 actually charged); check the incident history before reasoning about spend limits.
