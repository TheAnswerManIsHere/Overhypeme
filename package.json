{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:pull-prod": "pg_dump --clean --if-exists --no-owner --no-acl --no-comments \"$PROD_DATABASE_URL\" | psql \"$DATABASE_URL\"",
    "db:pull-prod:schema-only": "pg_dump --schema-only --clean --if-exists --no-owner --no-acl \"$PROD_DATABASE_URL\" | psql \"$DATABASE_URL\""
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "stripe": "20.0.0",
    "stripe-replit-sync": "1.0.0"
  }
}
