// @generated by scripts/generate-help-content.ts — DO NOT EDIT.
// Source of truth: docs/manual/. Run: pnpm --filter @workspace/overhype-me run generate:help

export const html = "<h1 id=\"chapter-12--background-work-async-jobs\">Chapter 12 · Background Work (Async Jobs)</h1>\n<blockquote>\n<p>How Overhype.me runs slow or external work — AI classification, image\ngeneration, email, image search, moderation refresh cycles — without making\nanyone wait on it, and how it stays visible to admins while it's happening.</p>\n<p>Deep spec: <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/architecture-map.md#async-jobs-and-queues\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>architecture-map.md</code></a>,\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/async-ui-status.md\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>async-ui-status.md</code></a>.\nRationale history: <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/decisions.md\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>decisions.md</code></a>.</p>\n</blockquote>\n<h2 id=\"what-it-does\">What it does</h2>\n<p>A lot of what Overhype.me does is too slow, too unreliable, or too expensive to\ndo inline while someone's request is waiting: classifying a fact's joke\nmechanism with an LLM, generating an AI image, sending a transactional email,\nfinding a <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#stock-image\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">stock photo</a>, re-running <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#enrichment\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">enrichment</a> on hundreds of facts at once,\nrepairing derived data. All of that runs as <strong><a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#background-work\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">background work</a></strong> — recorded in\nthe database rather than held in memory, so a server restart mid-run doesn't\nlose it. (One exception worth knowing: in a local setup with no email provider\nconfigured, an outgoing email is logged instead of being queued at all — see\nthe email section below.) <strong>Most</strong> of it is also\nretried automatically when it fails transiently. Not all: where a retry\ncouldn't actually finish the job — because a half-completed run can't be\nresumed — the queue deliberately opts out rather than spending attempts on\nsomething that cannot succeed. Which work that applies to, and why, is in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/architecture-map.md#async-jobs-and-queues\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>architecture-map.md</code></a>.</p>\n<p>Background work is invisible in the sense that a reader never sees a queue —\nbut every <strong>admin</strong> surface that triggers it (<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#taxonomy-health\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">Taxonomy Health</a>, moderation\nrenders, bulk actions) has to make that work's progress fully visible, because\nan admin is actively watching and deciding what to do next.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<h3 id=\"for-the-reader--user\">For the reader / user</h3>\n<p>Nothing is directly visible. A render or an email arrives after the action\nthat triggered it rather than holding that action up. There is no queue UI on\nthe consumer side.</p>\n<h3 id=\"for-the-admin\">For the admin</h3>\n<p>Every admin surface that kicks off background work must show status at <strong>two\naltitudes</strong> — per item (this specific fact's row: queued → working →\ndone/failed/<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#skipped\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">skipped</a>/still-running) and in aggregate (a running tally like\n\"7 of 25 done · 2 failed\"). A single spinner with no per-item detail is\nconsidered a bug, not a valid loading state. <strong>Taxonomy Health\n(<code>artifacts/overhype-me/src/components/admin/useTaxonomyHealthActions.ts</code>) is\nthe reference implementation</strong> — copy its\npattern rather than inventing a new one. The full contract, including why\n\"skipped\" and \"still running\" are first-class states and why the UI never\nimposes a timeout on a legitimately long job, is\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/async-ui-status.md\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>async-ui-status.md</code></a> — this chapter\ndoesn't restate it. The <strong><a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#bulk-media-backfill\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">Bulk Media Backfill</a></strong> panel (Admin → Taxonomy\nHealth) is a second reference implementation of the same <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#two-altitudes\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">two-altitude</a>\ncontract, for the corpus-wide stock-image and AI-meme backfill queues.</p>\n<h3 id=\"the-machinery\">The machinery</h3>\n<p>Everything rides <strong>one real database table</strong>, not an in-memory queue or a\nseparate pub/sub system. That single choice buys the property this whole\nchapter rests on: a crash or a redeploy never loses queued work, and at any\nmoment an ordinary SQL query shows the queue's recorded state — what is\nwaiting, what has been <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#claim\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">claimed</a>, what failed. (Recorded, not live: an\nindividual job carries no lease, so a job a crashed worker left behind still\nreads as claimed until a <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#recovery-sweep\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">recovery sweep</a> picks it up. Whether the <em>workers</em>\nthemselves are alive is tracked separately — see the known limitation below.)\nThe table's shape and the exact status flow are in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/architecture-map.md#async-jobs-and-queues\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>architecture-map.md</code></a>.</p>\n<p>Work is drained by <strong>independent <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#lane\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">scheduling lanes</a></strong>, and the important word is\n<em>independent</em>: a lane that is saturated cannot hold up another. What separates\nthem is <strong>who is waiting, and how much each job costs to run</strong>:</p>\n<ul>\n<li><strong><code>fast</code></strong> — pure-database admin actions with no AI or image call in the\npath. Someone clicked a button and is waiting to see it take effect.</li>\n<li><strong><code>render</code></strong> — single-item renders a moderator is watching a spinner for.</li>\n<li><strong><code>bulk</code></strong> — the catch-all for batch work nobody is watching in real time.</li>\n<li><strong><code>pexels</code></strong> and <strong><code>ai_meme_backfill</code></strong> — stock-image and AI-meme work for a\nfact. Both spend money or rate-limit budget at an external provider, so what\nmatters here is protecting the bill, not finishing quickly.</li>\n</ul>\n<p>That is what each lane is <em>for</em>. Everything quantitative about them — how many\nthere are, how often each polls, how much runs at once, which queue is\nassigned where, and what happens on retry — lives in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/architecture-map.md#async-jobs-and-queues\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>architecture-map.md</code></a>,\nand this chapter deliberately doesn't carry it. (See <em>the one bounded\nexception</em> in the <a href=\"/admin/help\" data-help-internal=\"/admin/help\">manual's charter</a> for why a chapter about\nmachinery may name the parts without quantifying them.)</p>\n<h3 id=\"email-the-most-consequential-rider\">Email, the most consequential rider</h3>\n<p>Transactional email — verification links, password resets, notifications —\nis queued like any other background work, but it is worth calling out\nbecause it is the one queue whose failures reach a real person's inbox, and it\ncarries three behaviors nothing else does:</p>\n<ul>\n<li><strong>A rejected API key stops further delivery attempts process-wide</strong> — and\nthat is not the same as pausing them. Once the provider rejects our\ncredentials, the app stops attempting delivery for the life of that\nprocess. (Not instantly to the last message: sends already in flight when\nthe rejection lands can still reach the provider. It is later attempts that\nare cut off, not in-progress ones.) Meanwhile each queued message that comes\nup for delivery still fails, still consumes one of its retry attempts, and\ncan exhaust its budget and be marked failed.\nSo rotating the key and restarting resumes sending, but it does <strong>not</strong>\nguarantee everything queued during the outage is still deliverable.</li>\n<li><strong>An abandoned email can alert the admins — if that alert is switched on.</strong>\nWhen a message exhausts its retries it would otherwise just be marked failed\nand forgotten, which matters because a silently undelivered password reset\nlooks identical to a user who never clicked. The alert is <strong>configuration-\ndependent</strong>: it fires only where the abandoned-email alert setting has been\nswitched on, so an environment that hasn't enabled it gets no notification.</li>\n<li><strong>That alert is exempt from the usual cleanup.</strong> Ordinary email rows are\npurged on a <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#retention\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">retention</a> schedule; the alert about a failed email is\ndeliberately kept, so the evidence outlives the thing it is evidence about.</li>\n</ul>\n<p>When delivery isn't configured at all (local development), the two directions\nbehave differently, and the distinction matters:</p>\n<ul>\n<li>A <strong>newly sent</strong> email is logged and returns — it is <strong>never queued</strong>, so it\nwill not be delivered later once a key is configured. Nothing is faked, but\nnothing is stored either.</li>\n<li>A row <strong>already in the queue</strong> is left pending and retried later rather than\nbeing consumed and failed, so genuinely queued work survives.</li>\n</ul>\n<h3 id=\"worker-liveness-and-the-queue-health-surface\">Worker liveness and the Queue Health surface</h3>\n<p>Every lane's worker publishes a <strong><a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#worker-lane-heartbeat\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">heartbeat</a></strong> — a small row saying \"instance\nX is still ticking lane Y, N jobs in flight\" — because the queue table alone\ncan't tell you a worker has died: a <code>pending</code> row looks identical whether a\nworker is about to claim it or every worker crashed an hour ago. Admin →\n<strong><a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#queue-health\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">Queue Health</a></strong> is the fleet-wide view built on those heartbeats, and it's a\n<strong>third</strong> reference implementation of the two-altitude contract above — this\ntime at the level of \"is the whole background-work system alive,\" not one\nqueue's items:</p>\n<ul>\n<li><strong>Aggregate altitude</strong> (<code>GET /api/admin/queue-health</code>) — every queue's raw\nstatus tallies plus two derived states, each from a different signal:\n<code>skipped</code> (a successful <code>done</code> row whose handler result says mid-run its\nwork no longer applied — nothing to do with attempts or the ceiling) and\n<code>abandoned_no_retry</code> (derived from comparing a <code>failed</code> row's attempts\nagainst its <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#retry-ceiling\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">retry ceiling</a>: the worker deliberately won't retry this one, a\ndifferent story from having retried repeatedly and given up). Per lane: how many\ninstances have a heartbeat row recent enough to\nstill count as live (not necessarily still actively ticking this exact\nsecond — a heartbeat can be silent past its own stale threshold but still\ninside the wider retention window), and whether the whole fleet has gone\nquiet on it.</li>\n<li><strong>Per-item altitude</strong> (<code>GET /api/admin/queue-health/jobs</code>) — the same drill-down\nevery queue gets, not just email.</li>\n<li><strong>A public liveness probe</strong> (<code>GET /api/health/queues</code>, unauthenticated) —\non total API-process death it's unreachable exactly like every other\nendpoint, so that's not what makes it useful. The aggregate endpoint above\nalready reports the same fleet-wide stall, as data in its JSON body behind\na 200. What's unique about the probe is turning that same verdict into\nthe <strong>HTTP status code itself</strong> — a meaningful unhealthy response <em>while\nthe process is still up</em>, so an external monitor can act on it without\nparsing a body — and it also fails closed (same unhealthy response) if\nchecking lane health itself errors out, rather than risking a false \"all\nclear.\" A single instance scaling down is normal, not an incident.</li>\n</ul>\n<h2 id=\"why-it-works-this-way\">Why it works this way</h2>\n<p>The lane split (2026-07, PR #216) replaced a single shared worker that\ndispatched <strong>all</strong> queues through one claim query, one concurrency pool, and\none shared \"is a tick already running\" guard. That worked fine when every\nqueue was roughly equally slow, but it produced real, reported head-of-line\nblocking: a cheap database-only admin click could sit in \"Queued…\" for 30+\nseconds because it was waiting behind slow AI/image-generation work — or\nbecause the <em>previous</em> worker tick hadn't finished processing a batch that\nhappened to include something slow. A moderator's test render had the same\nproblem in reverse: it could wait behind an unrelated bulk backfill batch.</p>\n<p>A split that folded renders into the same lane as batch work was considered\nand rejected, specifically so a render a moderator is actively watching\nnever has to wait on background batch work either — those are different\nenough in who's watching and how urgent they are to deserve separate\nisolation. The full decision, including the exact lane assignments and the\nconcurrency-default reasoning, is in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/decisions.md#2026-07--split-the-async-jobs-worker-into-fastrenderbulk-lanes\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>decisions.md</code></a>;\nthe general pattern (shared dispatcher + shared guard → head-of-line blocking)\nis written up as a reusable lesson in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/known-failure-patterns.md#head-of-line-blocking-in-a-shared-background-worker\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>known-failure-patterns.md</code></a>\nfor the next time a similar shared-worker design shows the same symptom\nelsewhere.</p>\n<h2 id=\"boundaries--known-limitations\">Boundaries &#x26; known limitations</h2>\n<ul>\n<li><strong>No priority <em>within</em> a lane.</strong> The lane split only isolates <em>between</em>\nlanes, not within one — a very large batch in the <code>bulk</code> lane still drains\nprogressively, not instantly. (The exact claim ordering is in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/architecture-map.md#async-jobs-and-queues\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>architecture-map.md</code></a>.)</li>\n<li><strong>The lanes and ordinary site traffic share one database connection pool.</strong>\nA job holds a connection while it is doing database work but not while it\nwaits on an outside service, so the number of jobs running is not the same\nmeasure as the number of connections in use — and nobody has measured the\nreal contention under load. The current sizing and the reasoning behind it\nare in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/architecture-map.md#async-jobs-and-queues\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>architecture-map.md</code></a>.</li>\n<li><strong>A crashed job is recovered, but on a deliberate delay.</strong> Work is never silently\nlost — a job whose process died mid-run is put back in the queue rather than\nstranded forever. But recovery runs <strong>on a deliberate delay</strong> rather than\nthe moment a job goes quiet, and the wait is not something to plan around:\na job must look stuck before the sweep will touch it, and a busy queue\npushes that further out. The delay is the safe choice, not an oversight.\nThe app <strong>can</strong> run as\nseveral instances at once, and a faster sweep would sometimes grab a job\nanother instance is still legitimately working on — running it twice. For an\nemail, that means a real person gets the message twice. Delayed recovery of\na rare crash is the\nbetter trade, and the mechanism that would let it be both fast and safe is\ntracked as follow-up work in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/engineering/deferred-work.md#code-level-tech-debt\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>deferred-work.md</code></a>.</li>\n<li><strong>Retention is not an audit log.</strong> Old <code>done</code>/<code>failed</code> rows are purged after\na configurable number of days per queue; <code>async_jobs</code> is operational state,\nnot permanent history.</li>\n<li><strong>A <code>done</code> job can still mean \"skipped.\"</strong> A handler can discover mid-run\nthat its work no longer applies (e.g. the target became ineligible in a\nrace) and complete successfully with a skip result rather than failing. Any\nUI reading job status has to distinguish this from a plain success — see the\n\"subtler version\" note in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/known-failure-patterns.md#async-enqueue-treated-as-completion\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>known-failure-patterns.md</code></a>.</li>\n<li><strong>The video pipeline doesn't run through this queue yet.</strong> A <code>fal_video</code>\nqueue exists in code as a placeholder for future work but isn't live.</li>\n</ul>\n<h2 id=\"going-deeper\">Going deeper</h2>\n<ul>\n<li><a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/architecture-map.md#async-jobs-and-queues\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>architecture-map.md</code></a> —\nthe current live queue list and lane assignments.</li>\n<li><a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/async-ui-status.md\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>async-ui-status.md</code></a> — the full\nstatus-surfacing contract every async-triggering UI must follow.</li>\n<li><a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/decisions.md#2026-07--split-the-async-jobs-worker-into-fastrenderbulk-lanes\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>decisions.md</code></a> —\nwhy the lane split happened and what was deliberately deferred.</li>\n<li>Code entry points: <code>lib/db/src/schema/asyncJobs.ts</code> (the table),\n<code>artifacts/api-server/src/lib/asyncJobs.ts</code> (the worker + lanes),\n<code>artifacts/overhype-me/src/components/admin/useTaxonomyHealthActions.ts</code> (the\nreference UI polling pattern), <code>artifacts/api-server/src/lib/factPexelsJobs.ts</code>\nand <code>artifacts/api-server/src/lib/aiMemeBackfillJobs.ts</code> (the <code>pexels</code> /\n<code>ai_meme_backfill</code> queue handlers),\n<code>artifacts/overhype-me/src/components/admin/useBulkMediaBackfillActions.ts</code>\n(the Bulk Media Backfill panel's polling hook),\n<code>lib/db/src/schema/workerLaneHeartbeats.ts</code> and\n<code>artifacts/api-server/src/lib/workerHeartbeats.ts</code> (the heartbeat table +\nwriter), <code>artifacts/api-server/src/lib/queueHealth.ts</code> and\n<code>artifacts/api-server/src/routes/health.ts</code> (the Queue Health queries +\nthe public probe), <code>artifacts/overhype-me/src/pages/admin/queueHealth.tsx</code>\n(the Queue Health page).</li>\n<li><a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/decisions.md#2026-07-30--queue-health-classification-persists-the-retry-ceiling-at-finalization-instead-of-re-deriving-it-live\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>decisions.md</code></a> —\nwhy the <strong>retry ceiling</strong> (not the <code>abandoned_no_retry</code> classification\nitself, which stays derived on every read) persists on the row at finalize\ninstead of being re-resolved live.</li>\n</ul>\n<p><strong>Next:</strong> this is the last chapter — back to the\n<a href=\"/admin/help#contents\" data-help-internal=\"/admin/help#contents\">contents</a>.</p>\n<p><em>Verified against <code>809079f</code> (2026-07-30) · claim inventory in PR #291.\nRe-grounded after PR #288 (async-queue hardening Phase 1) landed mid-review\nand falsified three claims here.</em></p>";
