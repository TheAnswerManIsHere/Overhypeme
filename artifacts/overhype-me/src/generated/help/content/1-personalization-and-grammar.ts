// @generated by scripts/generate-help-content.ts — DO NOT EDIT.
// Source of truth: docs/manual/. Run: pnpm --filter @workspace/overhype-me run generate:help

export const html = "<h1 id=\"chapter-1--personalization-and-grammar\">Chapter 1 · Personalization and Grammar</h1>\n<blockquote>\n<p>How one <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#fact\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">fact</a> template becomes a sentence about whoever's reading it — and\nstays grammatically correct no matter which name or pronouns that turns\nout to be.</p>\n<p>Deep spec: <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/token-rendering-and-grammar.md\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>token-rendering-and-grammar.md</code></a>.\nRationale history: <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/decisions.md\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>decisions.md</code></a>.</p>\n</blockquote>\n<h2 id=\"what-it-does\">What it does</h2>\n<p>A fact isn't stored as finished prose — it's stored as a template with\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#personalization-tokens\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">placeholders</a> for a name and for pronouns, plus verb forms that can go either\nway depending on which <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#pronoun-set\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">pronoun set</a> ends up filling them in. The same stored\ntemplate renders as a different, grammatically correct sentence for every\nreader: their name goes in, their pronouns go in, and every verb that needs\nto agree with those pronouns picks the right form automatically. Nobody\nwriting or approving a fact has to think about grammar across every\npossible pronoun set by hand — the system locks that guarantee in when the\nfact is written, and simply plays it back correctly for whoever ends up\nreading it.</p>\n<h2 id=\"how-it-works\">How it works</h2>\n<h3 id=\"for-the-reader\">For the reader</h3>\n<p>A visitor types a name on the home page, and Overhype.me makes its best\nguess at pronouns from that name — with presets (she/her, he/him, they/them)\nand a custom option always available to correct or replace the guess.\nEvery fact on the page re-renders instantly for whatever's currently\nselected — this is the personalization step the rest of the product's loop\nruns on. A registered user can also save default pronouns on their own\nprofile, so every fact renders that way for <em>them</em> while they browse —\nsigned in, without re-selecting each visit. It has no effect on how anyone\nelse sees that user's own submitted facts; every reader always personalizes\nfrom their own current selection, never the author's.</p>\n<h3 id=\"for-whoever-writes-the-words\">For whoever writes the words</h3>\n<p>A <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#submitter\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">submitter</a> or moderator doesn't need to hand-type the placeholders — the\nnormal path is to write in plain English, naming the subject the usual way,\nand let the system convert that into a template automatically. An advanced,\noptional editor still lets someone view or hand-adjust the converted result\nafterward, for the rare case that needs it. That conversion, and what a\nsubmitter sees happen to their own draft, belongs to\n<a href=\"/admin/help/2-content-lifecycle#for-the-submitter-writing-a-fact\"><code>2-content-lifecycle.md</code></a>\n(for a fact submission) and\n<a href=\"/admin/help/5-visual-pipeline\"><code>5-visual-pipeline.md</code></a> (for a moderator authoring a\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#visual-concept\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">Visual Concept</a>) — both routes end up going through the same underlying\nconversion, so a fact and a Visual Concept are personalized identically and\nnever drift apart from each other.</p>\n<h3 id=\"underneath-two-jobs-kept-strictly-separate\">Underneath: two jobs, kept strictly separate</h3>\n<p>Turning plain English into a template is one job; turning a stored template\ninto a sentence for a specific reader is a completely different one, and\nOverhype.me keeps them apart on purpose. The <strong>conversion</strong> step decides\nwhich words become placeholders and which verbs need to be able to change\nform — that's a one-time decision made when the text is written. The\n<strong><a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#rendering\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">rendering</a></strong> step, run fresh for every reader, only ever fills in what the\nconversion step already decided needed filling in — it never makes new\ngrammatical judgment calls of its own. Splitting the responsibility this\nway means a rendering bug can only ever be about substitution (the wrong\nword went in) and a conversion bug can only ever be about structure (the\nwrong thing was marked personalizable) — the two failure modes can't blend\ninto each other.</p>\n<h3 id=\"a-deterministic-pass-backs-up-the-ai-every-time\">A deterministic pass backs up the AI, every time</h3>\n<p>An AI model proposes the initial conversion from plain English to a\ntemplate, but the model's proposal is never trusted on its own. A fixed,\ncode-owned pass runs over every proposal afterward and corrects the\nspecific grammar mistakes a model proposal can make — a verb that should\nchange with pronouns but wasn't marked as such, one that was marked as\nchangeable but shouldn't be, that kind of thing. This same corrective pass\nalso runs on any template that reaches storage by a path that skipped the\nAI step entirely (an API submission, for instance), so a template can never\nend up stored half-corrected depending on which door it came through.</p>\n<h2 id=\"why-it-works-this-way\">Why it works this way</h2>\n<ul>\n<li><strong>A guaranteed correction pass matters more than a good prompt.</strong> An AI\nmodel can be instructed carefully and still occasionally get a pronoun\nagreement wrong — that's the nature of asking a model to reason about\ngrammar. Rather than trying to make the prompt perfect, Overhype.me\naccepts that the model will sometimes be wrong and runs a fixed,\ndeterministic pass afterward that catches the specific mistakes that\nmatter, every single time, regardless of how the model phrased its\nanswer.</li>\n<li><strong>Splitting conversion from rendering means a bug can only be in one\nplace at a time.</strong> If the same code both decided what was personalizable\n<em>and</em> filled in the actual words for a reader, a wrong sentence could\ncome from either job tangled together, and every bug report would start\nwith \"which part is broken?\" Keeping them separate means that question\nanswers itself: rendering never invents new structure, and conversion\nnever resolves an actual reader's pronouns.</li>\n<li><strong>A fact and a moderator's Visual Concept share the exact same\nconversion, on purpose.</strong> If fact submission and Visual Concept authoring\neach had their own version of \"turn plain English into a template,\" the\ntwo would eventually diverge — a grammar fix applied to one path and\nforgotten on the other. Routing both through one shared conversion means\na fix or an improvement is automatically true everywhere it's used.</li>\n</ul>\n<h2 id=\"boundaries--known-limitations\">Boundaries &#x26; known limitations</h2>\n<ul>\n<li><strong>A small set of older placeholder forms still render correctly for\nbackward compatibility</strong>, even though new writing never produces them —\nexisting facts stored under an earlier version of the system aren't left\nbroken by a later change to how new facts get written.</li>\n<li><strong>Article agreement (\"a\" vs \"an\") is fixed for a reader's actual name at\nrender time</strong>, since it depends on the specific name and can't be decided\nwhen the template is written; see the <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/token-rendering-and-grammar.md\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">spec</a>\nfor the exact rule and its known edge cases.</li>\n<li><strong>The system only ever fills in the placeholders a template actually\nhas</strong> — it doesn't reason about grammar beyond that; it substitutes and\npicks the correct pre-decided branch, nothing more creative than that.</li>\n</ul>\n<h2 id=\"going-deeper\">Going deeper</h2>\n<ul>\n<li>Spec: <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/token-rendering-and-grammar.md\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>token-rendering-and-grammar.md</code></a>\n— the exact token set, the pronoun-map mechanics, the full deterministic\ngrammar sequence, and the regression cases that must stay green.</li>\n<li>Related: <a href=\"/admin/help/2-content-lifecycle\"><code>2-content-lifecycle.md</code></a> (the submission\npath that produces a template), <a href=\"/admin/help/5-visual-pipeline\"><code>5-visual-pipeline.md</code></a>\n(the Visual Concept authoring path that shares the same conversion).</li>\n<li>Rationale: the <a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/glossary.md#tokenize\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\">tokenizer</a>/grammar entries in\n<a href=\"https://github.com/TheAnswerManIsHere/Overhypeme/blob/main/docs/ai-context/decisions.md\" target=\"_blank\" rel=\"noopener noreferrer\" data-external=\"true\"><code>decisions.md</code></a>.</li>\n</ul>\n<p><strong>Next:</strong> chapter 2 — <a href=\"/admin/help/2-content-lifecycle\"><code>2-content-lifecycle.md</code></a>, how\na fact gets into Overhype.me in the first place.</p>\n<p><em>Verified against <code>03efc05</code> (2026-08-09) · claim inventory in PR #367.</em></p>";
