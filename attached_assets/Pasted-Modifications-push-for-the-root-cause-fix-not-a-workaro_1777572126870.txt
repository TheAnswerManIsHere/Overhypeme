Modifications — push for the root-cause fix, not a workaround:
The task offers two options: (a) fix drizzle-kit migrate, or (b) document a script that wraps it. Tell Replit to start with (a).
	•	Diagnose first. The most likely cause: data-only migrations (INSERT/UPDATE without DDL) aren’t being tracked or applied by drizzle-kit migrate the same way as schema migrations. Drizzle has historically been DDL-focused; data migrations often need a different path. Confirm this is the case before writing code.
	•	Don’t accept “wrap it in a custom runner” as the first answer. That’s a workaround that calcifies the bug. If drizzle-kit migrate is silently skipping rows in the journal, the fix is either to make it apply them or to clearly separate DDL migrations from data migrations and run both via a documented pipeline. A custom runner that papers over the drift just moves the problem.
	•	If drizzle-kit genuinely cannot apply data migrations (Replit confirms this is by design): the right answer is a small explicit script (pnpm --filter @workspace/db run migrate:all) that runs DDL via drizzle-kit AND applies data files via psql, then verifies the journal matches the bookkeeping table. That’s option (b) done correctly — explicit, verified, idempotent. Not a “workaround.”
Concrete instruction to give Replit:
“First, diagnose why drizzle-kit migrate reports success while skipping 0028 and 0029. Specifically check whether those migrations contain data-only operations (INSERT/UPDATE) vs DDL. Report findings before changing code. Then fix the root cause: if drizzle-kit can be configured to apply them, do that. If drizzle-kit by design only handles DDL, write an explicit runner script that applies DDL via drizzle-kit and data files via psql, plus a verification step asserting the bookkeeping table matches the journal. Make the fix idempotent.”
Watch-items:
	•	Must include a verification step. The fix is not done until something asserts count(drizzle.__drizzle_migrations) matches length(journal). Without that, you’re just trusting a different “success” message.
	•	Don’t let Replit reset the dev DB to “fix” it. If you see anything like DROP DATABASE, truncate __drizzle_migrations, or “let’s start clean” in the diff, push back hard. The whole point is that re-runs are safe and idempotent, including against a DB that’s already partially applied.
	•	Confirm the fix runs on Replit’s standard dev startup. The task title says “apply automatically on dev startup” — make sure whatever Replit lands on is wired into the workflow that actually runs when you start a dev session, not just an opt-in script you’d have to remember to invoke.
	•	Make sure 0028 and 0029 actually get applied to your existing dev DB as part of this task, not left for a future “you should run this manually” follow-up. The fix should leave you in a known-good state, not just prevent future drift.
Net: clear accept, top of the priority pile alongside 271. These two — typecheck reliability and migration reliability — are the foundations the rest of your launch-readiness sits on. Get them right before you keep stacking features on top.